# Shell aggregate directory marker
# Individual modules (atuin.nix, tmux.nix, etc.) are auto-discovered by import-tree
# and merged into homeManager.shell aggregate namespace
{ ... }:
{
  # Empty stub - all configuration in individual module files
}
