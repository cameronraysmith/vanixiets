# AI aggregate directory marker
# Individual modules in claude-code/ are auto-discovered by import-tree
# and merged into homeManager.ai aggregate namespace
{ ... }:
{
  # Empty stub - all configuration in claude-code/*.nix files:
  # - claude-code/default.nix (programs.claude-code config)
  # - claude-code/mcp-servers.nix (8 servers active, 3 with secrets via sops-nix)
  # - claude-code/wrappers.nix (GLM wrapper with API key via sops-nix)
  # - claude-code/ccstatusline-settings.nix (3-line powerline status)
}
