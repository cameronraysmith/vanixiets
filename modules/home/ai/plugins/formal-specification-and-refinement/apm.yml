name: formal-specification-and-refinement
version: 0.0.0
description: Formal specification and refinement skills — Nucleus platform, refinement-driven development.
devDependencies:
  apm:
    # Intra-marketplace edge: refinement-driven-development builds on the
    # functional-programming-theory preferences. Recorded as an inert comment,
    # not an active dependency: the hard intra-marketplace edge is deliberately
    # not exercised, and `apm install` validates every transitive apm.yml and
    # rejects the NAME@MARKETPLACE shorthand ("Shorthand '@alias' is not
    # supported"), so an active entry breaks the hermetic compose. Both packages
    # are co-installed via the nix-generated root consumer manifest. Matches the
    # comment-only convention in planning-and-development/apm.yml.
    # - preferences-functional-programming-theory@vanixiets
