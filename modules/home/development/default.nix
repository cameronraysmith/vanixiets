# Development aggregate directory marker
# Individual modules (git.nix, jujutsu.nix, etc.) are auto-discovered by import-tree
# and merged into homeManager.development aggregate namespace
{ ... }:
{
  # Empty stub - all configuration in individual module files
}
