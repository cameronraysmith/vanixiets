workspace:
  slug: "cameronraysmith"
defaults:
  archive_documents:
    enabled: true
    title_prefix: "OpenSpec:"
  max_review_rounds: 3
teams:
  "CAM":
    id: "0ee3fb44-535c-47b7-b000-51617a6a3e18"
    name: "Cameron Smith"
projects:
  "cognee-memory-layer":
    id: "40537fb1-dadc-4405-ad3c-e5ed5dd7b66d"
    name: "Use knowledge graph for agent memory layer"
    teams: ["CAM"]
    archive_documents: {}
  "pyrite-baremetal-nixos":
    # The `id` below is the project slug fragment supplied at bind time, not a
    # verified Linear project UUID. The sibling entry carries a UUID. Reconcile
    # before the first archive-time document upsert, which resolves by id.
    id: "48b4123d589b"
    name: "Provision NixOS on bare-metal Apple hardware (pyrite)"
    teams: ["CAM"]
    archive_documents: {}
