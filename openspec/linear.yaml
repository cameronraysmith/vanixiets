workspace:
  slug: "cameronraysmith"
defaults:
  archive_documents:
    enabled: true
    title_prefix: "OpenSpec:"
  max_review_rounds: 3
teams:
  "CAM":
    id: "0ee3fb44-535c-47b7-b000-51617a6a3e18"
    name: "Cameron Smith"
projects:
  "pi-agent-environment":
    id: "d7d743a19249"
    name: "Pi agent environment"
    teams: ["CAM"]
    archive_documents: {}
  "cognee-memory-layer":
    # slugId, not the project UUID (40537fb1-dadc-4405-ad3c-e5ed5dd7b66d): `linear
    # document list --project` silently returns zero nodes with exit 0 for a UUID,
    # so the archive-time UPSERT's title scan misses and duplicates every document.
    id: "5c2059d671db"
    name: "Use knowledge graph for agent memory layer"
    teams: ["CAM"]
    archive_documents: {}
  "pyrite-baremetal-nixos":
    # The `id` below is the project slugId, and it must stay a slugId: the
    # archive-time document UPSERT resolves it via `linear document list
    # --project`, which resolves slugIds but silently returns zero nodes with
    # exit 0 for a project UUID. Substituting the UUID
    # (690ae4e7-f038-4ee4-bd7e-83557a68fb7c) makes the UPSERT's title scan miss
    # and duplicate every document on every archive, with no error to notice.
    id: "48b4123d589b"
    name: "Provision NixOS on bare-metal Apple hardware (pyrite)"
    teams: ["CAM"]
    archive_documents:
      "apple-laptop-hardware-support":
        id: "8d697b98-a0c8-43b3-9f2a-160da4278d4a"
      "bare-metal-install-path":
        id: "68768312-f4bd-4b3b-8fd9-f28a951a31d0"
      "encrypted-zfs-root":
        id: "ba0ffa20-a82b-40db-b292-d04f6a83eed2"
      "graphical-desktop-session":
        id: "8cf07dd5-2a48-4f45-bcd4-1add748aee46"
  "skill-evaluation":
    # The `id` below is the project slugId, and it must stay a slugId.
    # The archive-time UPSERT resolves it via `linear document list --project`.
    # It returns zero nodes with exit 0 for a project UUID, without erroring.
    # The UPSERT's title scan then misses and duplicates every document.
    # Nothing errors, so the duplication is only visible later in Linear.
    id: "88879c3180bd"
    name: "Skill evaluation"
    teams: ["CAM"]
    archive_documents: {}
