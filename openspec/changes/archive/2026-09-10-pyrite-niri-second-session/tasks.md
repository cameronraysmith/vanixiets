# tasks — niri as a second selectable session on pyrite (slice A)

**Follow-up F-niri-upstream = [CAM-66 — Correct password returns to GDM: niri survives a display-manager restart](https://linear.app/cameronraysmith/issue/CAM-66/correct-password-returns-to-gdm-niri-survives-a-display-manager)**; [excluded case, historical undischarged requirement and amendment record](known-limitations.md#verification-lesson-and-decided-undischarged-requirement).

Phase 1b implementation is written (2026-09-09): both niri-flake nixpkgs inputs follow root nixpkgs, pyrite imports the new `flake.modules.homeManager.niri`, and the paired niri/logind power-key policy is declared. See `logs/niri-slice-a-apply-phase1b.md` for exact-pin source checks, isolated schema evaluation, lock hashes, complete diffs, and evidence. The operator resolved all three phase-1 questions; the earlier lock entries were this change's own lock operation, not another agent's work.

Two standing rules from `openspec/config.yaml:24-30` apply to every verify line below: nix-managed outputs are not verified by reading source — verify by evaluating (`nix eval`), building (`nix build`), or inspecting the delivered path after activation, and say which. And any command over roughly 30 seconds or 10 lines of output is run as `<cmd> 2>&1 | tee logs/<lower-kebab>-$(date +%Y%m%d-%H%M%S).log`, with filters after the `tee`.

Ledger verification (2026-09-09): native evaluation/build used `git+file:///home/cameron/projects/vanixiets?ref=wip`, revision `28ecab070f0494706acda1bee519a0869d304a85`; pre-input baseline `eb20563e1f599f5194ac5e9a55cedc1ceae6c189`. Batched option/baseline evaluations and the negative overlay used `--impure`; ordinary CLI builds were pure, with a separate successful `--impure` toplevel build. Both output paths are recorded below. **Citation correction:** root input follows `nixpkgs_9 → 85f62611fa3f3eacbcfe3bc7a6d6518b443ca442`, identical to effective pyrite nixpkgs; `044bfe75…` belongs to another node merely named `nixpkgs`. The supersession claim in `logs/niri-slice-a-apply-phase1b.md:193-220` and distinction in `logs/niri-slice-a-ledger.md:18` were evidence-gathering errors, not a pin change. niri-flake remains `db2615fc6b3f75539ec681a984e3311b8d79ede0`. The historical ledger closed 17/31 tasks before deployment; its then-current `fbbjf4jv…` system is superseded by the runtime record below.

Final runtime evidence (2026-09-09): **23 checked / 9 unchecked**, including new post-deploy task 6.5. Authorized deployment completed at 19:00:04 UTC into `/nix/store/najhrzgayg05kd8bzm5g7n8rj74b7f80-nixos-system-pyrite-26.11.20260804.85f6261`; manual logind reload followed at 19:02:28 and GDM restart at 19:03:10. Operator observed interactive niri, Ghostty and clean exit. **cameron now has persisted `Session=niri`; the next login defaults to niri unless GNOME is selected with the cog.** Both orchestrator false negatives (unreadable AccountsService file labelled absent; system-only failed-unit query presented as global) are corrected in `verify.md` §9. No deploy/restart/suspend or writing git/jj command was run by this verifier. Evidence: `logs/niri-slice-a-verify.md`, `logs/niri-slice-a-verify-final.md`, and `verify.md`. Untested acceptance scenarios remain open, not converted into passing tests by the smoke test.

GNOME power-button amendment (2026-09-09, historical): **24 checked / 10 unchecked**, 34 total. New 4.5 verified the added GNOME value by resolved evaluation; 6.6 was then open for deployment/session restart/runtime readback. **Correction (2026-09-10): the fix IS deployed**, not merely committed: generation 10 and the live daemon's mapped database establish delivery/adoption (§9.8 of `verify.md`). Task 6.6 remains open only for its remaining prescribed in-session observations; 8.7 still requires physical tests in both GNOME and niri. Historical evidence: `logs/niri-slice-a-powerbutton-gnome-final.md`.

**Current finalisation gate: PASS WITH WARNINGS — archive-ready against the amended requirement, 34 checked / 4 unchecked.** Final independent comparison and explicit operator dispositions are in `verify.md` §9.12. **6.6 is runtime-discharged; 8.2 is split: remembered choice discharged, no-history GNOME is a method limitation, source-read only (not discharged by evaluation).** The operator declined destroying their AccountsService preference; a disposable no-history user would safely close that bounded gap. 8.2 remains unchecked because only half is discharged. Other unchecked tasks are 2.3 (vacuous by its rule), 8.6 (declined under CAM-59), and 9.3 (CAM-66 upstream follow-up). The CAM-66 amendment excludes only live-niri display-manager restart because niri v26.04 lacks GNOME 50.1's leader-death bridge, not an available configuration fix. **8.3/8.4/8.5 are evaluation, not observation; 8.7 is physical testing with same-boot positive/negative controls; whole-boot totals are 1 requested / 1 entry. Task 8.1 would have caught the strand; the checklist ordering itself caused the lockout inside niri.** The following verdict paragraphs are historical, not current dispositions.

Session-strand disposition (2026-09-10 UTC): historical **27 checked / 11 unchecked**; after discharging only 8.1 below, **28 checked / 10 unchecked**, 38 total. The journal-proven trigger was `sudo systemctl restart display-manager` at 00:12:06 while niri was live, after generation 10 was created at 00:11:53 and switch completed at 00:12:03. **[Operator] This was the orchestrator-supplied post-deploy sequence: the checklist itself contained the hazard, not a stray operator action.** GNOME authentication succeeded but startup aborted after niri survived session 157 removal; ordinary greeter switching is not an established trigger. The local monitor is removed from source by operator decision, not awaiting deployment; deployed source commit identity is not groundable. Section 9 and `known-limitations.md` retain **U-niri-fallback as UNSATISFIED**, with **F-niri-upstream = CAM-66** and open upstream task 9.3. Removal build/eval evidence: `logs/niri-slice-a-limitation-record.md`. **Never restart display-manager with live niri: quit (`Mod+Shift+E`, confirm) and require cameron's `niri.service=inactive` first; SSH/greeter location alone is insufficient.** Lingering and `moshi-hook` policy stay. **FAIL remains**; closing 8.1 or decision 9.4 does not satisfy the unconditional requirement.

Final verdict pass (2026-09-10 UTC): **32 checked / 6 unchecked**, 38 total. **FAIL — unchanged unconditional GNOME reachability/recovery remains UNSATISFIED (delta 39–43, 62–66; CAM-66).** Newly checked **8.3/8.4/8.5 are discharged by evaluation, not observation**, under the operator's explicit replacement of the original wall-clock method; **8.7 is discharged by operator physical testing plus independently re-derived journal/session evidence**. **8.6 is DECLINED**, not silently omitted, and remains unchecked. **2.3 remains unchecked by its explicit vacuity rule**. Non-vacuous **4.2, 6.6, 8.2 and upstream 9.3** remain open; no additional waiver was supplied. Task **8.1 was the runtime fallback test that sat unchecked while evaluation tasks were closed**; the **deploy → restart display-manager sequence itself caused the lockout when run inside live niri**. Evidence and precise boundaries: `verify.md` §9.9 and `logs/niri-slice-a-final-verdict.md`. No Nix edits or host mutations; toplevel rebuild skipped.

## 1. The flake input

- [x] 1.1 Add `epireyn/niri-flake` as a flake input with `inputs.nixpkgs.follows = "nixpkgs"`, and take nothing from it but `homeModules.config`. Do not import `nixosModules.niri`, do not use `packages.*`, do not enable `niri-flake.cache` — verified: native `nix flake metadata --json --no-write-lock-file` lists the node; local grep and committed-wip `git grep` show only the input/follows declarations and `homeModules.config` consumption. Native evaluation against the actual pre-input baseline and wip gives identical substituter lists (`substitutersEqual = true`), not merely a source inference. Evidence: `logs/niri-ledger-metadata-20260909-143250.log`, `logs/niri-ledger-history-20260909-143250.log`, `logs/niri-ledger-primary-source-20260909-143809.log`, `logs/niri-ledger-baseline-20260909-143809.log`, `logs/niri-ledger-baseline-comparison-20260909-143853.log`. The literal grep necessarily also lists input declarations; no package/module/cache consumption beyond `homeModules.config` is present. `design.md` D2.
- [x] 1.2 Confirm both of the input's nixpkgs inputs are followed rather than duplicated — verified by `nix flake metadata --json --no-write-lock-file`: `.locks.nodes["niri-flake"].inputs.nixpkgs` and `.nixpkgs-stable` both return `["nixpkgs"]`, and the added `nixpkgs-stable` node is absent. No additional nixpkgs tree remains from this input. Evidence: `logs/niri-slice-a-phase1b-metadata-clean-20260909-141237.log`. Source and a throwing-input schema test confirm `homeModules.config` does not evaluate `nixpkgs-stable`; following it is operator-approved.

## 2. The typed niri configuration in home-manager

- [x] 2.1 Import `epireyn/niri-flake`'s `homeModules.config` into cameron's home-manager and set its validation package explicitly to the system's niri, `config.programs.niri.package` from the NixOS side — verified by native evaluation of both `.home-manager.users.cameron.programs.niri.package.outPath` and `.programs.niri.package.outPath`: **both `/nix/store/y32xfvyx99qp91s2g3d2dr8wsx7k3gb0-niri-26.04`**, not just matching version strings. Evidence: `logs/niri-ledger-eval-20260909-143146.log`; actual config derivations also carry this exact build input in `logs/niri-ledger-baseline-comparison-20260909-143853.log`. `design.md` D3.
  - Re-read the locked `db2615fc…` input's `flake.nix:401-448` (`programs.niri.package`, config target, action/include helpers) and `settings.nix:1273-1311,2040-2046,3877` (includes and power-key option/renderer), not a default branch or historical report: `logs/niri-ledger-primary-source-20260909-143809.log`.
- [x] 2.2 Author a functionally sufficient `programs.niri.settings`: enough binds to spawn a terminal, move and close windows, and exit the session, plus this laptop's input settings — verified by native evaluation/build and reading **the store file**, `/nix/store/c3i3j0ss7bl6kixs9di4ghxzklh61vx8-config.kdl`: lines 37–53 contain 15 binds, including Ghostty spawn, close, focus/move and quit; lines 13–16 contain tap and natural scrolling. Evidence: `logs/niri-ledger-eval-20260909-143146.log`, `logs/niri-ledger-build-render-20260909-143250.log`. This discharges the rendered-config check, not physical terminal/compositor usability. `design.md` D5.
- [ ] 2.3 If any `settings.includes` entry is used, make it a store path — verify: `nix eval .#nixosConfigurations.pyrite.config.home-manager.users.cameron.programs.niri.settings.includes` returns entries every one of which begins `/nix/store/`. If no include is used, record that the requirement is vacuously satisfied and say so rather than checking a box on an empty list. `design.md` D6.
  - Native merged-host evaluation now returns `[]` (`logs/niri-ledger-eval-20260909-143146.log`): the store-backed-include requirement is **vacuously satisfied**, not exercised. Remains unchecked by this task's explicit empty-list rule; no operator action or artificial include is needed. An actual future include would require evaluation of every referenced store path before checking this box.
- [x] 2.4 Prove the validation actually runs and actually fails on bad input — verified natively: config `nix build … --no-link` succeeds; `nix log` reports `config is valid`. A throwaway in-memory `extendModules` expression adds `Mod+F12` with `ledger-invalid-action`; `nix eval --impure --raw --expr` resolves its `.drvPath`, then `nix build --impure --no-link -L --expr` fails with exit 1 and niri's ``expected `quit`, `suspend`, or one of 133 others`` diagnostic at that action. No overlay was written into repository source. Both actual derivations' builders run `niri validate -c $configPath` with the system package. Evidence and complete expression: `logs/niri-ledger-build-render-20260909-143250.log`, `logs/niri-ledger-negative-validation-20260909-143548.log`, `logs/niri-ledger-baseline-comparison-20260909-143853.log`. This is an expected negative test, not an implementation defect.
- [x] 2.5 Disable niri's power-key handling (operator decision D9) — verified by native config build, `nix eval --raw` of the same attribute, and store-file inspection: `/nix/store/c3i3j0ss7bl6kixs9di4ghxzklh61vx8-config.kdl` line 17 is the active `disable-power-key-handling` node inside `input`; the complete binds block has no power-key bind or suspend/poweroff action. `nix log` confirms validation passed with the exact system package (2.1/2.4). Re-read typed `input.power-key-handling.enable = false` at locked `db2615fc…` `settings.nix:2040,3877`; upstream owns attribute `niri-config`, target `niri/config.kdl`, with no second target declaration. Evidence: `logs/niri-ledger-build-render-20260909-143250.log`, `logs/niri-ledger-primary-source-20260909-143809.log`, `logs/niri-ledger-baseline-comparison-20260909-143853.log`. Rendered/build evidence only; live paired-policy test remains 8.7 and post-activation logind inspection remains 4.4.

## 3. System-level session registration

- [x] 3.1 Set `programs.niri.enable = true` beside the existing GNOME/GDM block — verified by native evaluation: enable is `true`, `services.displayManager.sessionData.sessionNames` is `["gnome","niri"]`, and `services.displayManager.generic.environment.XDG_DATA_DIRS` contains `/nix/store/k4cpgwdykkv2877gsk9grzsl7cppfp7g-desktops/share`, matching evaluated `sessionData.desktops`. The built display-manager unit carries that same directory. Evidence: `logs/niri-ledger-eval-20260909-143146.log`, `logs/niri-ledger-build-render-20260909-143250.log`. Session registration reaches GDM through this environment, not through `/run/current-system/sw/share/wayland-sessions`; greeter observation remains 8.1. `design.md` OQ3.
- [x] 3.2 Set `services.displayManager.defaultSession = null` **explicitly**, with the comment explaining why a plain definition beats nixpkgs' `mkDefault "niri"` while the option's own default does not — verified by native evaluation: `defaultSession = null` **and** `services.displayManager.generic.preStart = ""`; `systemd.services.display-manager.preStart` is also `""`. The built service has no `ExecStartPre` rewrite. Evidence: `logs/niri-ledger-eval-20260909-143146.log`, `logs/niri-ledger-build-render-20260909-143250.log`; explicit source/comment remains recorded in 3.3. This proves the configured `set-session` pre-start does not fire, not runtime AccountsService persistence (8.2). `design.md` D4.
- [x] 3.3 Confirm the choice of `null` over `"gnome"` is the one implemented — verified by `grep -n -A3 -B3 "defaultSession" modules/machines/nixos/pyrite/default.nix`: `null`, with a comment rejecting `"gnome"` because it rewrites every user's saved session on each GDM start. Evidence: phase1b format/diff log; evaluated behavior remains task 3.2.
- [x] 3.4 Record that the session picker needs no option — verified: the machine comment cites gnome-shell's `js/gdm/loginDialog.js:388-390` at tag 50.2; fetched that exact tag and confirmed `if (ids.length <= 1) { this._button.hide(); return; }`. Evidence: phase1b GNOME picker log. This is a source/comment check, not an observation of the greeter.

## 4. The never-auto-suspend guarantee and explicit power-key policy

- [x] 4.1 Carry explicit `services.logind.settings.Login.IdleAction = "ignore"` — native evaluation and unchanged lid keys were verified in `logs/niri-ledger-eval-20260909-143146.log`. Delivered-generation live D-Bus now returns `s "ignore"`; lids remain `lock`/`lock`/`ignore` (`logs/niri-verify-runtime-20260909.log:107-123`). This closes the requested post-deploy read; it does not prove an idle interval. `design.md` D7.
- [x] 4.2 Establish that no idle consumer is running or launched by the niri session — **DISCHARGED BY LIVE OBSERVATION, 2026-09-10 03:13:54 UTC**, via read-only `ssh pyrite.zt`, not configuration inference or the inherited capture. Full affected-user process list, running services, actual `xdg-desktop-autostart.target` and generated dependencies were inspected. Session **238**, uid **1000**, seat0, tty2, `gdm-password`, Wayland/user, active; niri PID **158454** remained active through the final session guard, GNOME manager inactive and GNOME Shell absent. **gsd-power, swayidle, hypridle, xss-lock, xidlehook and gsd-media-keys all absent**. Logind `IdleAction` is `s "ignore"`; zero failed user units. Evidence: `logs/niri-archive-live-snapshot-20260909-231353.log:1–97,246–272`; `verify.md` §9.11.
  - **Corrected predicate:** no idle consumer, not “nothing active.” IBus and benign keyring/autostart helpers are allowed; target is active. The pinned unit explicitly wants that target; the old `xdg-autostart*` wildcard was malformed. Lingering retains old `app-dbus-…org.gnome.*` slices, not running idle consumers (separate slice query: `logs/niri-archive-suspend-journal-20260909-231405.log:1–47`); this is the same persistent-user-manager condition underlying CAM-66, not a new failure. Empty session `Desktop=` is a minor unresolved propagation observation; source distinguishes `DesktopNames`/`XDG_CURRENT_DESKTOP` from PAM's `XDG_SESSION_DESKTOP` (§9.11). Whole-boot authentic totals independently remain **1 requested / 1 entry**; prior “2” counted sudo's query audit line and is corrected in the captured snapshot.
- [x] 4.3 Confirm no blanket suspend block was smuggled in as a substitute for D7/D9 — verified using read-only `git diff --no-ext-diff --unified=0 -- modules/machines/nixos/pyrite/default.nix` and the pre-edit snapshot (no writing jj/git commands permitted): no `AllowSuspend`, no masked `sleep.target`, no logind inhibitor, and no change to `disable-d3cold-all`, `nvme-d3cold-suspend-guard`, `disable-lid-wakeup`, `boot.kernelParams`, or `HandleLidSwitch*`. D9's explicit `HandlePowerKey = "ignore"` is intended, not a violation. Evidence: phase1b format/diff and protected-check logs. Blocking suspend outright would also strand the three D21 units that exist to make a deliberate suspend survivable.
- [x] 4.4 Declare `services.logind.settings.Login.HandlePowerKey = "ignore"` alongside unchanged lid settings, paired with 2.5 — native evaluation/rendering verifies it and leaves long press unset; evaluated systemd 261.1 defaults long press to `HANDLE_IGNORE` (`logs/niri-ledger-eval-20260909-143146.log`; `logs/niri-slice-a-ledger.md:19`). Delivered `/etc` and drop-ins were inspected with `systemd-analyze cat-config`; live short/long-press properties are both `s "ignore"` after the manual reload (`logs/niri-verify-runtime-20260909.log:107-123`), independently reread in final `verify.md` §9.9. No deprecated alias used. **File inspection was insufficient before reload:** running logind retained `poweroff` despite correct file contents. Task 6.5 records the effective-deployment obligation; physical 8.7 is now discharged separately.
- [x] 4.5 Disarm GNOME's independent power-key handler — verified with `nix eval .#nixosConfigurations.pyrite.config.programs.dconf.profiles.user.databases --json --apply 'dbs: map (db: { settings = db.settings; locks = db.locks; lockAll = db.lockAll; }) dbs'`, exit 0: `power-button-action="nothing"`, both sleep action types `"nothing"`, both signed timeouts `"0"`, unsigned `idle-delay="@u 1800"`, `locks=[]`, `lockAll=false`. Five power keys plus idle-delay, not six power keys. gsd 50.1's power schema names enum nick `nothing`; pinned nixpkgs serializes the bare Nix string as a GVariant string. Evidence: `logs/niri-powerbutton-after-dconf-20260909-193100.log` and `logs/niri-slice-a-powerbutton-gnome-final.md`. No locks declaration added; user overrides intentionally remain possible. This is evaluated/build-time evidence, not daemon state (6.6) or a physical press (8.7).

## 5. Leaving yesterday's change alone

- [x] 5.1 Confirm the GNOME and GDM power configuration is byte-identical — verified by native evaluation against committed pre-input baseline `eb20563e…`: all four power keys and `idle-delay` serialize identically (`dconfSerializedEqual = true`), with both action types `"nothing"`, signed timeouts `0` and unsigned idle delay `@u 1800`. Evidence: `logs/niri-ledger-baseline-20260909-143809.log`, `logs/niri-ledger-baseline-comparison-20260909-143853.log`. Rather than `jj diff` against a shared working copy, read-only committed `git show --unified=0 50b2c0dd… -- modules/machines/nixos/pyrite/default.nix` shows no protected hunk, and byte comparisons of the pre-input and pushed-wip protected block/autoSuspend line pass (`logs/niri-ledger-chain-20260909-143548.log`, `logs/niri-ledger-protected-20260909-143940.log`). This scoped equivalent excludes unrelated agents' work. `design.md` D8.
  - **GNOME amendment:** the historical byte-equality result above covered the four inactivity keys plus idle-delay, not power-button handling. Those five existing settings and `gdm.autoSuspend` remain unchanged; new `power-button-action="nothing"` is the sole policy addition, so whole-database byte equality is no longer claimed. D8 now explicitly permits this exception; current scoped diff and six-setting evaluation are in `logs/niri-slice-a-powerbutton-gnome-final.md`.
- [x] 5.2 Confirm the GNOME greeter database is unchanged — verified by native evaluation of `builtins.length …programs.dconf.profiles.gdm.databases`: **2 before, 2 after**, using committed pre-input baseline `eb20563e…` and pushed wip `28ecab07…`, not the modified configuration relabelled as baseline. Evidence: `logs/niri-ledger-baseline-20260909-143809.log`, `logs/niri-ledger-baseline-comparison-20260909-143853.log`; evaluated `gdm.autoSuspend` remains `false`.

## 6. Build and deploy

- [x] 6.1 Instantiate the whole host configuration, walking assertions and option collisions — verified natively with `nix eval …#nixosConfigurations.pyrite.config.system.build.toplevel.drvPath`: pure evaluation succeeds with `/nix/store/r7xk9ph8ck3db4hi8q5n9rism2442fga-nixos-system-pyrite-26.11.20260804.85f6261.drv`; `--impure` succeeds with `/nix/store/aaafl4rn6zd9b1f99ck5mlsiafpggqkb-nixos-system-pyrite-26.11.20260804.85f6261.drv`. Both evaluated at immutable wip revision `28ecab07…`; the evaluation-mode difference is recorded, not attributed to this slice without evidence. Evidence: `logs/niri-ledger-pure-impure-20260909-143548.log`.
- [x] 6.2 Build it — verified natively by `nix build …#nixosConfigurations.pyrite.config.system.build.toplevel --no-link`: pure build succeeds at `/nix/store/7gb7kpaji4gcfnla3p8479qjc76ldj0w-nixos-system-pyrite-26.11.20260804.85f6261`; separate `--impure` build reproduces the supplied `/nix/store/l4isdhx40m500gj726cm8w7qiwffx5q2-nixos-system-pyrite-26.11.20260804.85f6261`. Read the built system's `etc/systemd/system/display-manager.service`, followed its `XDG_DATA_DIRS` to `/nix/store/k4cpgwdykkv2877gsk9grzsl7cppfp7g-desktops/share/wayland-sessions/`, and listed/read both `gnome.desktop` and `niri.desktop`. Evidence: `logs/niri-ledger-build-render-20260909-143250.log`, `logs/niri-ledger-pure-impure-20260909-143548.log`. Build was not activation: the ledger then observed `fbbjf4jv…`; the subsequent deployment is recorded in 6.3.
  - **GNOME amendment rebuild (historical):** `nix build .#nixosConfigurations.pyrite.config.system.build.toplevel --no-link` succeeded, exit 0, at `/nix/store/akq1sxw1bisvqhsbz5j5ik245zpky2xk-nixos-system-pyrite-26.11.20260804.85f6261`; full output `logs/niri-powerbutton-toplevel-build-retry-20260909-193307.log`. First attempt timed out at 120 seconds; retry completed. That worker built, not activated, the shared working copy. **Subsequent generation 10 demonstrably delivers the power-button fix and niri** (`verify.md` §9.8); no claim maps that generation to this build or a source commit.
- [x] 6.3 Deploy — authorized deployment is established by host journal: `clan-boot-pyrite-1087ad37.service` and `clan-switch-pyrite-40b2afd1.service` deactivated successfully; `finished switching to system configuration /nix/store/najhrzgayg05kd8bzm5g7n8rj74b7f80-nixos-system-pyrite-26.11.20260804.85f6261` at **19:00:04 UTC** (`logs/niri-final-package-20260909.log`, activation-unit section). Subsequent SSH and `/run/current-system` matching that generation were reproduced by the predecessor (`logs/niri-verify-runtime-20260909.log:2-6`) and SSH works in this read-only pass. This is remote activation-success evidence, not a recovered local `clan machines update` exit transcript; no such transcript was found and no update was rerun. No reboot occurred here; the warm-reboot Wi-Fi hazard remains.
  - **Current delivery, re-derived 2026-09-10:** `/run/current-system` and system profile generation 10 resolve to `/nix/store/pgww7gi7jxb3ibdbfa2mphb2azid0znx-nixos-system-pyrite-26.11.20260804.85f6261`, dated **00:11:53 UTC**; switch finished **00:12:03**. The delivered file-db carries `power-button-action='nothing'` plus the unchanged sleep/idle settings, and niri 26.04 is present. `configuration-revision` is absent: delivered configuration is evidence, commit identity is not. `logs/niri-cam66-final-runtime-20260910.log:39–70`.
- [x] 6.4 Restart the greeter so it picks up the new session list — historical journal shows stop at 19:03:04 and start at **19:03:10 UTC**, later than activation completion at 19:00:04 (`logs/niri-final-portal-context-20260909.log:959,994-1011`); predecessor's `ActiveEnterTimestamp` agrees (`logs/niri-verify-runtime-20260909.log:9`). Operator then saw the cog and logged into niri. No restart was issued by this verifier. **Future operational gate: never restart display-manager while niri is live. Quit niri first, or arrange the authorized restart from GNOME/the greeter, and require cameron's `systemctl --user is-active niri.service` to print `inactive`.** Merely running from SSH or being at the greeter is insufficient. Warn the user: restart terminates graphical sessions; switch alone does not refresh GDM's list. `known-limitations.md` records why and how to recover.
- [x] 6.5 **Post-deploy: reload logind and verify daemon state before relying on D9.** During an authorized deployment, run `sudo systemctl reload systemd-logind` (not restart), then read `IdleAction`, `HandlePowerKey` and `HandlePowerKeyLongPress` using `busctl get-property org.freedesktop.login1 /org/freedesktop/login1 org.freedesktop.login1.Manager <property>`; all must return `s "ignore"`. Inspect delivered config/drop-ins and unchanged lids as in 4.4. **Deploying this change does not fully take effect without this reload or a reboot; no reboot is authorized by this task.** Completed for this deployment by the orchestrator's manual reload at 19:02:28, corroborated by sudo and `Config file reloaded.` journal entries (`logs/niri-final-portal-context-20260909.log:983-987`) and predecessor's live values (`logs/niri-verify-runtime-20260909.log:107-123`). Before reload the orchestrator observed `HandlePowerKey=poweroff`; reading only the already-correct file would have falsely passed. This agent did not perform the reload.
- [x] 6.6 **Post-deploy effective-session power policy — DISCHARGED BY RUNTIME OBSERVATION and explicit operator disposition (2026-09-10).** Inherited generation-10 delivery and `/proc/148129/maps` establish GNOME `gsd-media-keys` adoption of the new file-db, not merely a later process start; the original GNOME prerequisite transcript was not collected in its prescribed order. The operator now dispositions the residual through the live niri compositor's own `/proc/158454/environ`, re-derived at **03:22:14 UTC**, session **238**: `XDG_CURRENT_DESKTOP=niri`, `XDG_SESSION_ID=238`, `XDG_SESSION_DESKTOP=niri`; `gsettings get org.gnome.desktop.session idle-delay` → `uint32 1800`; listing **all** `org.gnome.settings-daemon.plugins.power` keys → `No such schema` (exit 1). **That schema is not on niri's XDG_DATA_DIRS at all: GNOME power settings are structurally inapplicable in this niri session, not merely unread by an absent daemon.** Four inhibitors are delay-mode `sleep` from ModemManager, NetworkManager, rtkit-daemon and upowerd; **no block-mode `handle-power-key` inhibitor**, independently corroborating 8.7's niri half (GNOME's `gsd-media-keys` holds exactly that inhibitor and bypasses logind). `/etc/dconf/profile/user` names the generation-10 file-db. `Desktop=` remains empty and unresolved. Source: `logs/niri-final-session-observation-20260909-232214.log:1–44`; `verify.md` §9.12. No restart/reload/deploy performed; future database adoption must still be verified through the consuming daemon, and any authorized display-manager restart must obey 6.4.
  - **Restart safety correction:** any display-manager restart in 6.6 is subject to 6.4's no-live-niri gate. The known limitation is not repaired; neither deployment nor this task authorizes reproducing it on pyrite.

## 7. Formatting and scope

- [x] 7.1 Format only the files this change owns — verified: `treefmt --walk filesystem --fail-on-change --no-cache flake.nix flake.lock modules/machines/nixos/pyrite/default.nix modules/home/niri/default.nix openspec/changes/pyrite-niri-second-session/tasks.md` reports `formatted 3 files (0 changed)`, exit 0; `nixfmt --check` on the three Nix files also exits 0. The installed treefmt config only formats Nix and excludes lockfiles; no Markdown formatter is configured. No `just lint` or repository-wide formatter ran. Evidence: phase1b format/diff log.
- [x] 7.2 Confirm the change touched only what it claims — verified with the routed-chain equivalent of the unusable join-wide `jj diff --stat`: read-only `git diff --no-ext-diff --stat a34162bf401e1b8db4d0d98fcdc7a414c28d9690 50b2c0dd152e7279a323495a48bcd662daccd1bf` lists exactly nine paths: `flake.nix`, `flake.lock`, pyrite's module, `modules/home/niri/default.nix`, and the five artifacts in this change directory. **Nothing under `openspec/specs/`.** Evidence: `logs/niri-ledger-scoped-stat-20260909-143809.log`; chain ancestry/commits in `logs/niri-ledger-chain-20260909-143548.log`. This checks this change's routed delta, not the other agents' active join. No writing jj/git command ran; the delta spec still syncs at archive.
  - The historical ledger pass edited only `tasks.md` and ignored logs. This final pass edits `tasks.md`, `design.md`, new `verify.md` and authorized findings/command logs only; exact list in `logs/niri-slice-a-verify-final.md`. Other agents' aggregate working-copy edits are neither touched nor treated as defects.

## Integration Verification

- [x] 8.1 Two desktops are actually offered, and a person can sign in to either — verify: at the machine's own panel, the GDM greeter shows a session cog with both GNOME and Niri listed; sign in to niri and reach an interactive compositor; sign out; sign in to GNOME and reach an interactive shell. `systemctl is-active display-manager` returning `active` is not evidence — the living capability already records that GDM reports `active` while the panel is blank and while a session restart-loops. Signing in to niri also closes the one residual `design.md` OQ3 left open: niri's desktop entry uses a relative `Exec=niri-session`, and no prior work traced `gdm-session-worker`'s exec of a Wayland session end to end.
  - **Discharged: operator panel observation plus independently re-derived live session evidence.** The inherited operator cog/Niri/Important Hotkeys/Ghostty/command/clean-quit smoke is corroborated by `logs/niri-verify-runtime-20260909.log:210–233`, closing design OQ3's relative `Exec=niri-session` residual. The operator's successful GNOME fallback login is now corroborated by session **176**, uid **1000**, **seat0**, `gdm-password`, Wayland, user, active, **tty2**; running **GNOME Shell 50.2**; active `gnome-session-manager@gnome.service`; inactive `niri.service`. Read-only evidence: `logs/niri-cam66-final-runtime-20260910.log:1–37` and `logs/niri-cam66-final-daemon-20260910.log:1`. SSH corroborates the living session, not visual panel interaction. This is recovered GNOME usability, not an uninterrupted replay of the original smoke or proof of unconditional fallback.
  - **Verification-process finding:** task 8.1 is the runtime GNOME fallback test that would have caught the session-strand defect before the operator hit it in the affected post-restart state. It sat unchecked while other tasks were discharged by evaluation. Runtime observation must not be deferred because it is inconvenient. The original quit-only smoke need not reproduce the live-niri restart counterexample. Discharging this recovered login does not erase the incident or repair **U-niri-fallback / CAM-66**. The pre-amendment **FAIL** remains the correct historical verdict (`verify.md` §§9.7–9.9); task 9.4 now explicitly narrows the requirement after that counterexample, with the independent current verdict in §9.10.
- [ ] 8.2 **SPLIT by explicit operator disposition (2026-09-10): remembered choice DISCHARGED; no-recorded-history half is a METHOD LIMITATION, source-read only.** Privileged read at **03:22:14 UTC** shows `/var/lib/AccountsService/users/cameron` with `[User] Session=niri`, modified at the current niri login **03:08:17 UTC** and still present. The predecessor's login-time write at **2026-09-09 19:04:57** and subsequent session evidence establish persistence across logins, not an absent file (`logs/niri-verify-discrepancies-20260909.log:1–13`; new `logs/niri-final-session-observation-20260909-232214.log:19–33`). This discharges the remembered-choice half under the operator's disposition; it does not claim an unchanged inode/mtime across intentional GNOME/niri choices or a newly performed controlled restart comparison.
  - **No-history GNOME is NOT discharged and MUST NOT be tested by deleting cameron's record.** The operator declined destruction of their real saved preference to demonstrate a default. GNOME-default-for-no-history rests on exact GDM **50.1** source (`gdm-session.c:621–625,687–694`) and `services.displayManager.defaultSession = null` with evaluated empty preStart (3.2), **not observation and not discharged-by-evaluation**. This is a known, bounded method limitation, explicitly accepted as non-blocking, not an unverified omission. **Safe future discharge: a second disposable user with no session history**, followed by an ordinary no-cog login. No account or preference was modified here.
  - The former empty-AccountsService and restart sequence is retained as historical method context in §9.11, not an outstanding authorization. Any future authorized restart must first quit niri and confirm the affected user's `niri.service=inactive` (6.4); SSH/greeter location alone is insufficient.
- [x] 8.3 The niri session does not suspend the host on mains power — **DISCHARGED BY EVALUATION, NOT OBSERVATION**, by explicit operator decision (2026-09-10). The original method was an untouched AC interval of at least 35 minutes with SSH/journal continuity; that interval was **not performed**. Exact-tag niri configuration scan has zero idle matches; delivered KDL starts no idle consumer; delivered user-unit/autostart dependencies contain none; logind D-Bus says `IdleAction=ignore`. This establishes no configured idle-suspend mechanism, independently of GNOME, not a behavioral 35-minute test or proof against future user-started daemons. See `verify.md` §9.9 for executed evidence and the unrecovered historical gsd-power/process-snapshot boundary.
- [x] 8.4 The niri session does not suspend the host on battery — **DISCHARGED BY EVALUATION, NOT OBSERVATION**, by explicit operator decision (2026-09-10), replacing this task's former refusal of configuration-only discharge. The same absence-of-consumer/logind policy is power-source independent; it is not borrowed from GNOME. **No charger-unplugged, untouched 35-minute interval was observed.** This is an explicit verification-method disposition, not a claim that the original behavioral check ran; the idle/power delta requirements remain unchanged by the separate CAM-66 amendment. Evidence/boundaries: `verify.md` §9.9.
- [x] 8.5 GNOME and the greeter still do not suspend the host — **DISCHARGED BY EVALUATION, NOT OBSERVATION**, by explicit operator decision (2026-09-10). Fresh file-db-only reads of both delivered user and GDM profiles give both sleep action types `'nothing'` and both timeouts `0`; exact gsd 50.1 `idle_configure()` guards independently prevent registering the sleep watch. Live logind remains `ignore`. No repeated untouched GNOME/greeter wall-clock windows were performed. Whole-boot totals are **1 request / 1 entry**, solely the pre-fix 21:16:36 media-keys press, hence zero idle-attributed and zero post-fix requests, **not literal whole-boot zero**. Evidence: `verify.md` §9.9.
- [ ] 8.6 A deliberate suspend still works from the niri session — original behavioral method: with separate informed consent and physical presence, deliberately suspend from niri; observe both `disable-d3cold-all` and `nvme-d3cold-suspend-guard`, suspend entry, and record resume outcome.
  - **DECLINED by operator decision (2026-09-10), not awaiting scheduling or silently unverified.** CAM-59 already records the resume defect: the cited historical cohort is 8 failures / 38 cycles (about one in five), with physical power-cycle recovery. Re-proving that defect adds no useful evidence for this change at that risk. No deliberate niri suspend, guard transition or wake observation is claimed; no blanket suspend block was introduced (4.3), but that is not the behavioral test. Leave unchecked rather than count declined execution as passed. This also dispositions the wake subcheck in 8.7; do not initiate an extra suspend. Risk source/count boundary: `verify.md` §9.9.
- [x] 8.7 Verify the ordinary-press policy in **both GNOME and niri** — **DISCHARGED by operator physical tests and independently re-derived journal/session attribution (2026-09-10)**, not configuration-only evidence. GNOME session **176** started **00:16:22**, after deployment; its press is **02:02:54.328864**, before logout **02:03:08**. Fresh niri session **209**, compositor PID **154670**, started **02:03:36** and loaded the delivered KDL; its presses are **02:03:39.311662** and **02:03:41.429194**, before confirmed quit **02:03:50.852104**. Logind saw all three `Power key pressed short.` events. The test window has **0 suspend requests / 0 PM entries / no shutdown request**, stable boot identity and realtime-minus-monotonic offset (2 μs spread). Operator supplies physical use/usability; journal grounds which desktop was live, and read-only SSH corroborates continued reachability. Evidence: `verify.md` §9.9.
  - **Same-boot controls:** pre-fix press **21:16:36.327747 → suspend requested from `.gsd-media-keys` at 21:16:36.392292 → PM entry 21:16:37.863260** is the positive control; protected presses logged with no suspend/shutdown are the negative control. Logind seeing the key rules out “the events never occurred.” Whole-boot totals remain **1/1**, not zero; no post-test sleep gap. Task 6.6's residual is now separately runtime-discharged under explicit operator disposition (§9.12), not retroactively collected in its original order. Deliberate suspend/wake subcheck is **declined under 8.6**, not observed, and no long hold was tested.
- [x] 8.8 X11-only applications are known not to work, and that is recorded rather than discovered — verify by actual niri-session DISPLAY/PATH inspection **or the compositor's direct failed-spawn/disabled-integration diagnostic**. Slice A does not install `xwayland-satellite` (`design.md` OQ1); upstream would start it on demand if available. Equivalent direct runtime evidence is recorded below, without pretending the originally suggested shell commands ran.
  - **Discharged by niri's own runtime diagnostic, not a claimed interactive shell command:** `error spawning xwayland-satellite at "xwayland-satellite", disabling integration: No such file or directory (os error 2)` and immediately `Environment variable $DISPLAY not set, ignoring.` (`logs/niri-verify-runtime-20260909.log:226-227`). This directly establishes failed executable resolution and disabled integration; the SSH lookup also found no satellite. No in-session `echo` is claimed. Known limitation remains design OQ1 / **B-xwayland**, not a slice-A defect/task.

## 9. Known login-session limitation (local monitor declined)

- [x] 9.1 Establish and record the actual upstream lifecycle defect, exact trigger, operational rule and recovery — niri v26.04 lacks the leader-death bridge GNOME 50.1 implements with its FIFO monitor (selected gnome-session-ctl 50.0); systemd 261.1's graphical target already has `StopWhenUnneeded=yes`. Journal proves the 00:12:06 UTC display-manager restart removed session 157 while niri survived until manual SIGTERM at 00:15:16. The ordinary-greeter-switching attribution was inference and is refuted. `known-limitations.md` carries primary file citations, the no-live-niri restart gate, affected-user SSH recovery, deliberate linger/`moshi-hook` policy, and the operator's reasons for not carrying the monitor. No runtime experiment was performed by this worker.
- [x] 9.2 Remove the entire monitor and niri lifecycle overrides; delete change-local `monitor-tests.py`; preserve research/prototype only as upstream-report log material; verify unchanged protected module settings, original nixpkgs unit shape and native toplevel build — `logs/niri-slice-a-limitation-record.md`. Build and both unit evaluations exit 0; the built system has no monitor unit. This supersedes the formerly checked implementation task: its historical build/mocks remain evidence about a **withdrawn**, never end-to-end-verified prototype, not a shipping repair. All operational instructions in 6.4, 6.6 and 8.2 now carry the no-live-niri restart gate.
- [ ] 9.3 Submit the exact-trigger upstream niri report with v26.04/GNOME FIFO comparison, journal evidence and explicitly unverified prototype archive (`known-limitations.md`). **F-niri-upstream = CAM-66**, linked at the top of this ledger, tracks the excluded, unrepaired **U-niri-fallback** case. The Linear follow-up exists per operator metadata; no upstream niri submission is yet established. Record the upstream URL/status there when actually submitted. Reporting alone does not repair the defect. Real GDM/systemd failure injection belongs only in a separately authorized disposable environment, not pyrite; no local-daemon deployment is required. Task 8.1 records recovered two-desktop usability, not a lifecycle repair.
- [x] 9.4 **Spec-owner decision applied: narrowly AMEND the requirement after the CAM-66 counterexample.** This supersedes the earlier retain-unchanged/UNSATISFIED decision without rewriting its historical FAIL or incident evidence (`logs/niri-slice-a-undischarged-record.md`). Apply the exact draft in `logs/niri-slice-a-amend-and-verdict.md:57–88`: GNOME registered/selectable at the greeter and default for users with no recorded choice; explicitly exclude the state caused by restarting display-manager while niri is live; quit niri and require the affected user's `niri.service=inactive` before restart. Only the recovery scenario's WHEN is narrowed; THEN/AND remain byte-for-byte, including sign-out rather than repair and no advance preparation outside the exception. This corrects an upstream lifecycle promise no configuration of ours can supply: niri v26.04 lacks GNOME 50.1's leader-death bridge, rather than an available configuration fix being omitted. **CAM-66 / U-niri-fallback remains an excluded, unrepaired follow-up**, not silently discharged. No monitor reinstated. Current independent verdict and strict/scope evidence: `verify.md` §9.10 and `logs/niri-slice-a-amend-applied-verdict.md`.
