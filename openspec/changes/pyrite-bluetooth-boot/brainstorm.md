# Approved experiment

This captures the already-approved preparation decision, not a new research round.
Pyrite's native Bluetooth UART has failed initialization after boot with timeout -110 and no adapter.
An earlier successful native reload prevents attributing recovery to the wake patch; repeating that intervention is outside this change's preparation authorization.

Prepare one persistent experimental module on the exact audio baseline `5f88cb1ff50857614e5a0613386b4fd786dd3061`.
Reuse the reviewed Linux 6.18.42 module-only build method and historical six-line omission from christophgysin/linux `ddf622a0a19697af473051c8019fffc1eb66efe7`, also used by leifliddy/macbook12-bluetooth-driver `d393bf988644f0ce24f57c031febe0d9a4256f89`.
The selected kernel's sources, prepared configuration, compiler and seven native UART constituents remain authoritative.

Preparation includes tracked package, patch, Pyrite-only extra-module contribution, focused aggregate-selection check and store builds.
It excludes full kernel/compiler compilation, deployment, bootloader/profile changes, reboot, driver reload, service changes and all Bluetooth/audio interactions.
GNOME headset success and niri silence are historical observations; niri silence remains separately unresolved.
Linear binding is explicitly deferred without a fabricated issue ID.

A parent independent review precedes operator-coordinated source-based deployment and an attended fresh boot.
The candidate must be attested before any manual intervention, followed by adapter, pairing and acoustic acceptance in GNOME and niri under separate authorization.
One success cannot establish optimality or long-term suspend/reboot reliability.
