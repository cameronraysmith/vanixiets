# Execution plan

This materializes the authorized preparation plan without reopening design choices.
Keep the existing single Bluetooth delivery directly on exact B `5f88cb1ff50857614e5a0613386b4fd786dd3061`.
Route only owned paths/deltas downward from the same integrated wip, preserving the delivery Change-Id and foreign join arms.
The join-growth step in the earlier plan is already complete and must not be repeated.

## Source and check cycle

First evaluate the focused check against frozen B and observe the missing persistent candidate assertion.
Then add the selected-kernel recipe, tracked six-line patch and single Pyrite extra-module line; evaluate the frozen own delivery rather than `@`.
Validate zero-fuzz patch applicability and exact behavior parity with the reviewed scratch patch.
Check the candidate diff against B independently to exclude DankGreeter/GNOME removal and every other foreign change.

## Native build cycle

Authenticate to `cameron@pyrite.zt` without altering network or SSH configuration.
Use local evaluation and exact derivation/input closure transfer, not a remote checkout/ref push or evaluation of remote dirty `@`.
Dry-run every module/check/tree/initrd/system build group before starting it.
Stop if full kernel/compiler compilation or exceptional expensive missing inputs are required; cached prerequisite restoration and normal module/system builds are authorized.
Use finite managed daemon jobs, builders empty, max-jobs 1, cores 2, complete tee/pipefail logs and explicit exits.

Build the module, record its actual post-strip SHA256/build ID and compare metadata/aliases/symbols and relevant BTF layouts with reviewed scratch/native.
Build the full Pyrite aggregate tree and run Bluetooth/audio checks; reject a native-selection negative probe.
Confirm the kernel derivation and output remain identical to B.
Build and inspect the actual initrd, including module selection, absence of new Bluetooth preload, normalized non-Bluetooth contents and crypto/LUKS support; realize the exact toplevel without activation.
Record all immutable refs, derivations and outputs, distinguishing evaluated output paths from realized ones.

## Separate acceptance gates

Independent review and operator coordination precede normal source-based deployment.
No activation, bootloader/profile change, reboot, driver reload, service/radio/audio operation, publication or archive is part of this preparation.
Retain recovery Generations 10 and 11.
Fresh candidate boot identity must be attested before manual intervention; adapter/pairing and acoustic GNOME/niri observations follow only under separate authorization.
There is no automated equivalent for these physical acceptance gates, and they remain real pending obligations rather than inferred successes.
One positive observation cannot establish optimality or long-term suspend/reboot reliability.
