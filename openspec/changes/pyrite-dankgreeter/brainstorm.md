# Selected approach

The operator authorized preparation and build of C after the source-reviewed, Linux-build-verified B candidate, without activation.
The pinned research at `/Users/crs58/.local/state/atomic/pyrite-desktop/run-o0V32M/research.json` supplies the source and lifecycle analysis.
This captures the supplied decisions rather than inventing another discovery cycle.

Native nixpkgs DankGreeter and greetd replace declared GDM/GNOME.
The existing epireyn pin remains a typed configuration interface only; native nixpkgs provides all runtime packages.
An isolated home-manager configuration renders greeter KDL, and the installed niri validates it.
Native greetd PAM authenticates the desktop user; the separate greeter account uses native dms-greeter PAM.
No autologin, authentication wrapper, password capture, extra shell owner or mutable configuration input is introduced.
GNOME's effective supporting-service defaults must be retained explicitly.
Physical acceptance is separate from preparation and remains pending.

The operator selected direct execution in the shared jj development join instead of the schema's workflow, nested-agent and worktree instructions.
The authorized scope is two existing Nix files and this change directory, with one local delivery commit and private build transfer only.
