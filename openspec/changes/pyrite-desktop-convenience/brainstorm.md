# Approved scope

The operator approved two consecutive preparation slices: declarative auto-hide for the existing default DankBar, then install-only Zen Browser for cameron on pyrite.
Use the existing DMS settings object and a dedicated Home Manager package aggregate imported only by pyrite.
Pin youwen5/zen-browser-flake at `4036109214cf20632000558935bd823b901fa886`, following root nixpkgs.
Do not change browser defaults, profiles, extensions, policies, other browsers or session lifecycle.
Native finite builds are authorized; activation and physical acceptance are not part of preparation.
