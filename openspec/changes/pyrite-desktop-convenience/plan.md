# Delivery plan

A1 appends the default-bar setting and focused desktop assertion after a missing-setting RED evaluation.
Verify rendered settings and retained assertions, then freeze the first delivery commit.

A2 appends the pinned input, minimal lock dependencies, dedicated Home Manager aggregate, pyrite-only import and package/desktop-entry checks.
Verify exact package scope, unchanged defaults and input pins, then build the frozen candidate's Zen package, desktop check and system closure natively with bounded resources.
Reuse unchanged audio, Bluetooth, kernel and initrd evidence only after confirming artifact identity.

Route only owned paths into each new commit; preserve the existing eleven commits and shared development join.
Return exact refs and logs without activation or external publication.
