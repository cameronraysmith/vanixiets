# Tasks

Each task names its verification.
Stage numbers match proposal.md; tasks within a stage depend on the preceding tasks of that stage unless stated, and each stage depends on the one before it.
Stage 0 is this change.
Decisions D-S1, D-S2, D-P1, D-P2, D-C1, D-C2, D-M1 (design.md Open Questions) gate the tasks that name them.

## 0. Design (this change)

- [x] 0.1 Record research findings and option analysis in ADR-007 — verify: `docs/notes/development/kubernetes/decisions/ADR-007-nixos-vm-tests-for-k3s.md` exists, is indexed in `docs/notes/development/kubernetes/README.md`, and every path:line citation resolves in the reference trees at the revisions its appendix lists
- [x] 0.2 Write this change's brainstorm, proposal, design, delta specs, and tasks — verify: `direnv exec . openspec status --change k3s-nixos-vm-tests --json` reports `tasks` as `done` and `direnv exec . openspec validate k3s-nixos-vm-tests` passes, and each capability named in proposal.md has a `specs/<capability>/spec.md`
- [ ] 0.3 Obtain answers to D-S1, D-S2, D-P1, D-P2, D-C1, D-C2, D-M1 — verify: the answers are recorded in design.md Open Questions and the gated tasks below are unblocked or rewritten

## 1. Substrate regulators

- [ ] 1.1 (D-M1) Land the containerd correction as its own module PR if D-M1 says before — verify: `nix eval` of the standalone module shows either `virtualisation.containerd.enable = true` with the settings rendered or `services.k3s.containerdConfigTemplate != null`, and the inert `virtualisation.containerd.settings` block is gone
- [ ] 1.2 Add `modules/checks/k3s-server-eval.nix` (T1) asserting the evaluated module's `ExecStart` flags, kernel modules, sysctls, firewall lists, and containerd shape — verify: `nix build .#checks.x86_64-linux.k3s-server-eval` passes on a host without KVM; removing `--disable-kube-proxy` from `modules/nixos/k3s-server/default.nix` makes it fail naming the flag; revert
- [ ] 1.3 Add a T1 leaf for the `local-k3d-ci` nixidy environment beside `nixidy-env-local-k3d` in `modules/checks/nixidy-k8s.nix` — verify: `nix build .#checks.x86_64-linux.nixidy-env-local-k3d-ci` passes and its output contains `file:///manifests`
- [ ] 1.4 (D-S1) Add `modules/checks/vm-k3s-single-node.nix` following `vm-nixos-base.nix`: one machine importing `flake.modules.nixos.k3s-server` (and `base` if D-S1 says yes) with `enable`, `clusterInit`, store-path `tokenFile`, `memorySize = 1536`, `diskSize = 4096`, no CNI — verify: `nix build .#checks.x86_64-linux.vm-k3s-single-node 2>&1 | tee logs/vm-k3s-single-node-$(date +%Y%m%d-%H%M%S).log` passes on the KVM host; record wall time in the PR body
- [ ] 1.5 Write the single-node test script asserting the T2 rows of ADR-007 Q1: service active, node registered, `Ready=False` with `reason == KubeletNotReady` and message containing `cni plugin not initialized`, CoreDNS `Pending`, no flannel/cni0 link, no `cni-` netns, no kube-proxy process or `KUBE-SERVICES` chain, `br_netfilter`/`nf_conntrack`/`overlay` loaded, `ip_forward`/`bridge-nf-call-iptables`/`overcommit_memory` values, TCP 6443 accepted and an unlisted port dropped, kubeconfig unreadable by `noprivs`, `k3s-killall.sh` output free of `command not found` and service inactive afterwards — verify: the leaf passes; each assertion's stdout appears in the test log
- [ ] 1.6 Record mutation evidence for the single-node leaf: set `net.ipv4.ip_forward = 0` in `kernel.nix`, rebuild, capture the failure; remove `6443` from `networking.nix`, rebuild, capture the failure; revert both — verify: both failure outputs and the revert are in the PR body and `git diff main -- modules/nixos/k3s-server/` is empty
- [ ] 1.7 Confirm the buildbot worker's behavior on the new `kvm`-requiring leaf — verify: buildbot's evaluation of the stage 1 branch reports every pre-existing check with an unchanged verdict and the VM leaf as absent or skipped, per `ci-log-verification`; if it reports a failure, add the `requiredSystemFeatures` filter to the buildbot-nix configuration in the same stage

## 2. Multi-node join

- [ ] 2.1 Add `modules/checks/vm-k3s-multi-node.nix`: `server` (`role = "server"`, `clusterInit = true`) and `agent` (`role = "agent"`, `serverAddr = "https://<server ip>:6443"`) on one `virtualisation.vlans = [ 1 ]`, shared `pkgs.writeText` token, `services.k3s.nodeIP` set as glue on each machine — verify: `nix build .#checks.x86_64-linux.vm-k3s-multi-node 2>&1 | tee logs/vm-k3s-multi-node-$(date +%Y%m%d-%H%M%S).log` passes on the KVM host; wall time and peak memory recorded in the PR body
- [ ] 2.2 Write the multi-node test script: both nodes listed by `kubectl get node` on the server, agent `ExecStart` contains `--server=` and neither `--cluster-cidr` nor `--service-cidr`, agent reaches `server:6443` and `server:10250` — verify: the leaf passes with each assertion's output in the log
- [ ] 2.3 Record mutation evidence: remove `6443` from the firewall list, rebuild, confirm the agent never registers and the leaf fails at the registration assertion; revert — verify: failure output in the PR body
- [ ] 2.4 (D-S2) If the fleet adopts a shared `k3s-token` clan vars generator, switch the leaf to clanTest's in-sandbox vars generation and delete the store-path token — verify: `rg writeText modules/checks/vm-k3s-multi-node.nix` returns nothing and the leaf passes

## 3. Platform stack in a VM

- [ ] 3.1 (D-P1, D-P2) Add the `local-vm` nixidy environment in `modules/nixidy.nix` and `kubernetes/nixidy/` derived from `local-k3d`: `nixidy.target.repository` pointing at the node's `file://` bare repository, hostnames embedding the VM node address, CoreDNS `hosts`/`template` answering them, `SopsSecret` payloads encrypted to the test-only public key, the production Gateway address mechanism from D-P1 — verify: `nix build .#nixidyEnvs.x86_64-linux.local-vm.environmentPackage .#nixidyEnvs.x86_64-linux.local-vm.bootstrapPackage` passes; `rg 1.1.1.1 result/` returns nothing
- [ ] 3.2 Add T1 leaves `nixidy-env-local-vm` and `nixidy-bootstrap-local-vm` to `modules/checks/nixidy-k8s.nix` — verify: both build without KVM
- [ ] 3.3 Pin `alpine/curl:latest` by digest in the module that references it, and add a T1 image-inventory check that fails on any floating tag in the rendered `local-vm` environment — verify: the check passes; reintroducing `:latest` makes it fail naming the reference; revert
- [ ] 3.4 Add fixed-output `dockerTools.pullImage` derivations by digest for every image in the ADR-007 Q4 inventory plus the k3s pause and CoreDNS images, and measure the closure — verify: `nix path-info -S` on the set is recorded in the PR body; the compressed total is compared against the 1.5–2.5 GiB bound in ADR-007 F4
- [ ] 3.5 Add the committed test-only age keypair outside `modules/` (for example `kubernetes/tests/fixtures/age/`) with a README stating it is a test fixture and what it does not cover, and an activation script installing the private key on the VM node — verify: `gitleaks` in `prek run --all-files` passes with the fixture allow-listed by path, and the README lists production key provisioning, recipients, rotation, and `SOPS_AGE_KEY` wiring as uncovered
- [ ] 3.6 Add `modules/checks/vm-k3s-platform.nix`: one node importing `k3s-server`, `services.k3s.images` from 3.4, `services.k3s.manifests` carrying Cilium and the foundation, activation creating the bare repository from the `local-vm` environment package, the age fixture, and `pkgs.chainsaw` in the guest; sizing starts at `memorySize = 6144; diskSize = 16384` — verify: `nix build .#checks.x86_64-linux.vm-k3s-platform 2>&1 | tee logs/vm-k3s-platform-$(date +%Y%m%d-%H%M%S).log` passes on the KVM host; wall time recorded
- [ ] 3.7 Write the platform test script: wait for `k3s.service`, apply the app-of-apps, then run `KUBECONFIG=/etc/rancher/k3s/k3s.yaml chainsaw test <store path of kubernetes/tests/local-k3d>` in the guest and copy the report out — verify: Chainsaw exits zero; the report is in the test output; no `k3d-wait-*` logic is ported into the script
- [ ] 3.8 Record mutation evidence: edit the Gateway assert to expect five listeners, rebuild, capture Chainsaw's `infrastructure-gateway` failure, revert; remove the `CiliumLoadBalancerIPPool` (or D-P1 equivalent), rebuild, capture `AddressNotAssigned`, revert — verify: both failures in the PR body
- [ ] 3.9 If 3.6's wall time exceeds fifteen minutes, split `vm-k3s-cilium` out per design D6 — verify: the split change records the measured time and each leaf passes independently

## 4. Execution and deletion

- [ ] 4.1 Add a `workflow_dispatch`-only probe job to `.github/workflows/test-cluster.yaml` on `ubuntu-latest` that applies the `/dev/kvm` udev rule, runs `ls -l /dev/kvm`, and builds `vm-k3s-single-node` with `--option system-features 'kvm nixos-test benchmark big-parallel'` — verify: three consecutive dispatches pass with run links recorded in the change, or the failure is recorded and D-C1 is invoked
- [ ] 4.2 (D-C1) Add a `vm` job on the probed runner label building every `checks.x86_64-linux.vm-k3s-*` leaf through `cached-ci-job` with `hash-sources` extended by `modules/checks/vm-k3s-*.nix modules/nixos/k3s-server/** kubernetes/nixidy/** kubernetes/tests/fixtures/age/**`, uploading the Chainsaw report — verify: the job passes on a push; a sysctl change in `kernel.nix` produces a cache miss on the next run
- [ ] 4.3 Rewire `.github/workflows/release.yaml`'s `test-cluster` call to the `vm` job — verify: the release workflow's `test-cluster` step reports the VM job's result
- [ ] 4.4 (D-C2) In one commit separate from 4.2 and 4.3, and only after `vm-k3s-platform` has passed in CI on the chosen runner: delete `modules/apps/cluster/k3d-integration-ci.sh`, `k3d-full.sh`, `k3d-wait-ready.sh`, `k3d-wait-argocd-sync.sh`, `k3d-bootstrap-secrets.sh`, `k3d-configure-dns.sh`, `k3d-test-coverage.sh`, `scripts/k3d-test-coverage.sh`, the `integration` job and `SOPS_AGE_KEY` line in `test-cluster.yaml`, the `local-k3d-ci` environment and its T1 leaf from 1.3, and the k3d justfile recipes only the CI path used; retain `kubernetes/clusters/local-k3d/` unless D-C2 says otherwise — verify: `rg k3d-integration-ci .github justfile modules scripts` returns nothing; `nix flake check` on the closure passes on the KVM host; reverting the commit restores the k3d job with the `vm` job unaffected
- [ ] 4.5 Update `docs/notes/development/kubernetes/workflows/06-testing.md` and `README.md` to describe the VM regulators and the deletion — verify: `prek run --all-files` passes and the docs name every `vm-k3s-*` leaf that exists

## Integration Verification

- [ ] IV.1 On the KVM developer host, `direnv exec . just test-integration 2>&1 | tee logs/test-integration-$(date +%Y%m%d-%H%M%S).log` builds `vm-nixos-base`, `vm-k3s-single-node`, `vm-k3s-multi-node`, and `vm-k3s-platform` from a clean store and every leaf passes — verify: the log ends with each leaf's store path
- [ ] IV.2 `nix eval .#checks.aarch64-darwin --apply builtins.attrNames` lists no `vm-*` leaf — verify: command output
- [ ] IV.3 The buildbot worker's evaluation of the final branch reports every non-VM check with an unchanged verdict and no VM leaf as a failure — verify: `ci-log-verification` on the buildbot run
- [ ] IV.4 The chosen GitHub Actions runner's `vm` job passes on the final branch and `test-cluster.yaml` contains no `k3d` reference — verify: workflow run link and `rg k3d .github/workflows/test-cluster.yaml` empty
- [ ] IV.5 The mutation evidence for stages 1–3 is present in the merged PR bodies and each mutated file is byte-identical to `main` after revert — verify: PR body links and `git diff main -- modules/nixos/k3s-server/ kubernetes/tests/` empty
- [ ] IV.6 Every requirement in the three delta specs is discharged by a named leaf, job, or review rule, and the three world assumptions are cited by the requirements that rest on them — verify: `direnv exec . openspec validate k3s-nixos-vm-tests` passes and a traceability pass over the specs finds no undischarged requirement
