# Tasks

Each task names its verification.
Stage numbers S0–S5 match proposal.md and ADR-007; tasks within a stage depend on the preceding tasks of that stage unless stated, and each stage depends on the one before it except S5, which depends only on S3's rendering leaves.
Stage D is this change.
Ambiguities A1–A9 (design.md) gate the tasks that name them; S4 additionally requires Cameron's explicit approval to spend (A9) and is never started by silence.

## D. Design (this change)

- [x] D.1 Record research findings, the assertion-to-tier table, and the option analysis in ADR-007; record the reconciler and artifact decisions in ADR-008; record the node-management, multi-cloud, and networking decisions in ADR-009 — verify: the three files exist under `docs/notes/development/kubernetes/decisions/`, are indexed in `docs/notes/development/kubernetes/README.md`, every path:line citation resolves in the reference trees at the revisions ADR-007's appendix lists, and each ADR's provenance table maps every design-review decision code to an ADR decision
- [x] D.2 Write this change's brainstorm, proposal, design, six delta specs, and tasks — verify: `direnv exec . openspec validate k3s-nixos-vm-tests` passes, and each capability named in proposal.md has a `specs/<capability>/spec.md` whose requirements each name a coverage bin and non-vacuity evidence
- [ ] D.3 Obtain answers to A1–A9 — verify: the answers are recorded in design.md Ambiguities and the gated tasks below are unblocked or rewritten

## S0. Purity and provenance regulators (KVM-free)

- [ ] S0.1 Add the `k3s-server.snapshotter` option (default `"nix"`, emits `--snapshotter <value>`, puts `pkgs.nix` on the unit path) and delete the inert `virtualisation.containerd.settings` block in `modules/nixos/k3s-server/` — verify: `nix eval` of the standalone module shows `--snapshotter nix` in `ExecStart` and no `virtualisation.containerd` attribute; `git diff` touches only `modules/nixos/k3s-server/`
- [ ] S0.2 Add `modules/checks/k3s-server-eval.nix` (T1) asserting the evaluated module's `ExecStart` flags including `--snapshotter nix`, kernel modules, sysctls, firewall lists, unit path, and containerd shape — verify: `nix build .#checks.x86_64-linux.k3s-server-eval` passes on a host without KVM; removing `--disable-kube-proxy` from the module makes it fail naming the flag; setting `snapshotter = "overlayfs"` makes it fail naming `--snapshotter nix`; revert both
- [ ] S0.3 Add the easykubenix cluster module skeleton under `kubernetes/` with the cloud-invariant core and the `platform` sum typed over `hetzner | gcp | aws | kubevirt`, `hetzner` implemented, `gcp`/`aws` as render-only stubs, `kubevirt` as a not-implemented error (A6) — verify: `nix eval .#<cluster>.platform` evaluates for `hetzner`, `gcp`, `aws`; `builtins.tryEval` on `azure` and `kubevirt` returns `success = false` with the messages the `capi-cluster-rendering` spec names
- [ ] S0.4 Add T1 leaves `k8s-manifest-purity` and `k8s-images-preloaded` over the rendered tree (`flakeRef`, `nixExpr`, `:latest`, untagged, tag-without-digest; rendered images ⊆ preload set derived by string context) — verify: both pass; reintroducing `alpine/curl:latest` makes `k8s-manifest-purity` fail naming the reference; adding a `Deployment` with an unpreloaded image makes `k8s-images-preloaded` fail naming the image; revert both
- [ ] S0.5 Add the OCI-layout derivation for the rendered tree (`oci-layout`, `index.json`, `blobs/sha256/`) with the manifest digest written to `$out/digest`, and the T1 leaf `k8s-closure-provenance` — verify: `nix build --rebuild` of both yields byte-identical outputs; `crane manifest` over the layout reports the digest in `$out/digest`
- [ ] S0.6 Add the Flux install derivation (`flux install --export --components=source-controller,kustomize-controller,notification-controller` from `pkgs.fluxcd`) and T1 leaves `flux-install-rendered` and `flux-sources-pinned` — verify: both pass; adding `helm-controller` to the components makes `flux-install-rendered` fail; rendering the root `OCIRepository` with `spec.ref.tag` only makes `flux-sources-pinned` fail; revert both
- [ ] S0.7 Add T1 leaves `capi-cloud-invariant-render`, `capi-platform-sum-total`, `capi-ccm-present`, `clustermesh-preconditions` — verify: all four pass; reading `config.platform.hetzner.region` from a core object fails `capi-cloud-invariant-render` with a diff; deleting the Hetzner CCM fails `capi-ccm-present`; declaring overlapping PodCIDRs fails `clustermesh-preconditions`; revert each
- [ ] S0.8 Add `apps.<system>.push-cluster-artifact` (E) that pushes the layout to GHCR under the flake-revision tag, reads back the registry digest, and exits non-zero on inequality; do not run it against GHCR in this stage — verify: a dry run against a local `docker-distribution` on the developer host prints equal digests and exits zero; pushing a different layout under the same tag exits non-zero naming both digests

## S1. Substrate and snapshotter leaves

- [ ] S1.1 Add `modules/checks/vm-k3s-single-node.nix` following `vm-nixos-base.nix`: one machine importing `flake.modules.nixos.base` and `k3s-server` with `enable`, `clusterInit`, store-path `tokenFile`, `memorySize = 1536`, `diskSize = 4096`, no CNI — verify: `nix build .#checks.x86_64-linux.vm-k3s-single-node 2>&1 | tee logs/vm-k3s-single-node-$(date +%Y%m%d-%H%M%S).log` passes on the KVM host; wall time recorded in the PR body
- [ ] S1.2 Write the single-node test script asserting the T2 rows of ADR-007 Q1 plus the snapshotter rows: service active, node registered, `Ready=False` with `reason == KubeletNotReady` and message containing `cni plugin not initialized`, CoreDNS `Pending`, no flannel/cni0 link, no `cni-` netns, no kube-proxy process or `KUBE-SERVICES` chain, `br_netfilter`/`nf_conntrack`/`overlay` loaded, sysctl values, TCP 6443 accepted and an unlisted port dropped, kubeconfig unreadable by `noprivs`, `k3s-killall.sh` clean, `k3s ctr plugins ls` lists `io.containerd.snapshotter.v1 nix ok`, the generated containerd config names `nix` as CRI snapshotter, NRI plugin state logged as an observation — verify: the leaf passes; each assertion's stdout appears in the test log; the NRI observation is quoted in the PR body
- [ ] S1.3 Record mutation evidence for the single-node leaf: set `net.ipv4.ip_forward = 0`, rebuild, capture; remove `6443` from the firewall, rebuild, capture; set `snapshotter = "overlayfs"`, rebuild, capture the plugin assertion failure; revert all — verify: the three failure outputs and the revert are in the PR body and `git diff main -- modules/nixos/k3s-server/` is empty
- [ ] S1.4 Add `modules/checks/vm-k3s-nix-workload.nix`: single node as S1.1 with a test-only flannel override as CNI glue, one pod whose image is `nix-snapshotter.buildImage` referenced `nix:0<store path>` — verify: the pod reaches `Running` and `kubectl exec` shows the store-path binary; removing `pkgs.nix` from the unit path makes the pod fail with an error naming `nix-store`; revert
- [ ] S1.5 If S1.2 observed NRI disabled, set `services.k3s.containerdConfigTemplate` in the module to enable it and extend S1.2 to assert `[plugins."io.containerd.nri.v1.nri"] disable = false`; otherwise record that the default template suffices — verify: the module diff or the recorded observation is in the PR body
- [ ] S1.6 Add `modules/checks/vm-k3s-multi-node.nix`: `server` (`clusterInit = true`) and `agent` (`role = "agent"`, `serverAddr`) on one VLAN, shared `pkgs.writeText` token, `services.k3s.nodeIP` as glue — verify: `nix build .#checks.x86_64-linux.vm-k3s-multi-node 2>&1 | tee logs/vm-k3s-multi-node-$(date +%Y%m%d-%H%M%S).log` passes; both nodes listed, agent `ExecStart` has `--server=` and no CIDR flags, agent reaches 6443 and 10250; removing 6443 from the firewall makes the agent never register; revert
- [ ] S1.7 Confirm the buildbot worker's behavior on the `kvm`-requiring leaves — verify: buildbot's evaluation of the S1 branch reports every pre-existing check with an unchanged verdict and each VM leaf as absent or skipped, per `ci-log-verification`; if it reports a failure, add the `requiredSystemFeatures` filter to the buildbot-nix configuration in the same stage

## S2. Platform leaf: Flux from an OCI layout through an offline registry

- [ ] S2.1 Port the `local-k3d` cluster content (Cilium, Gateway API CRDs, cert-manager, step-ca, ClusterIssuer, Gateway, test Certificate) into the easykubenix cluster module's core; render ArgoCD and sops-secrets-operator no longer; vendor third-party images by digest — verify: `nix build` of the rendered tree passes S0.4's leaves; `rg -i argocd kubernetes/ --glob '!kubernetes/clusters/local-k3d/**'` returns nothing
- [ ] S2.2 Add the committed test-only age keypair and cosign keypair under `kubernetes/tests/fixtures/` with a README stating they are test fixtures and listing what they do not cover — verify: `gitleaks` in `prek run --all-files` passes with the fixtures allow-listed by path; the README lists Clan vars generation, recipients, rotation, and `SOPS_AGE_KEY` wiring as uncovered
- [ ] S2.3 Add nix2container or fixed-output pull derivations for every image in the rendered tree plus pause and CoreDNS, and measure — verify: `nix path-info -S` on the set is in the PR body and compared against the 1.5–2.5 GiB bound in ADR-007 F4; `k8s-images-preloaded` passes
- [ ] S2.4 Add `modules/checks/vm-k3s-platform.nix`: one node importing `base` and `k3s-server`, `services.k3s.images` from S2.3, `services.k3s.manifests` carrying Cilium, the Flux install, a `CiliumLoadBalancerIPPool`, the `sops-age` and cosign public-key Secrets from S2.2, and the root `OCIRepository`/`Kustomization` with the digest from S0.5; a `docker-distribution` registry in the guest (A4) seeded at activation by `crane push` from the layout; `pkgs.chainsaw` in the guest; sizing starts at `memorySize = 6144; diskSize = 16384` — verify: `nix build .#checks.x86_64-linux.vm-k3s-platform 2>&1 | tee logs/vm-k3s-platform-$(date +%Y%m%d-%H%M%S).log` passes on the KVM host; wall time recorded
- [ ] S2.5 Write the platform test script: wait for `k3s.service`, wait for the root `Kustomization` `Ready=True` with `lastAppliedRevision` ending in the S0.5 digest, then run `KUBECONFIG=/etc/rancher/k3s/k3s.yaml chainsaw test <store path>` and copy the report out — verify: Chainsaw exits zero; the report is in the test output; no `k3d-wait-*` logic is ported
- [ ] S2.6 Update the Chainsaw suite: replace the ArgoCD steps with Flux `Kustomization` assertions and the sops-secrets-operator step with a decrypted-Secret assertion; keep cert-manager, step-ca, ClusterIssuer, Gateway — verify: S2.5 passes with the updated suite
- [ ] S2.7 Record mutation evidence: swap `spec.ref.digest` for a different layout's digest, capture the `OCIRepository` fetch failure; sign with a foreign cosign key, capture `SourceVerified=False`; edit the Gateway assert to five listeners, capture the Chainsaw failure; remove the `CiliumLoadBalancerIPPool`, capture `AddressNotAssigned`; delete the `sops-age` Secret, capture the decryption failure; revert all — verify: the five failures are in the PR body
- [ ] S2.8 If S2.4's wall time exceeds fifteen minutes, split `vm-k3s-cilium` out per design D6 — verify: the split change records the measured time and each leaf passes independently

## S3. Management-cluster handlers and the CAPI bootstrap seam

- [ ] S3.1 Add `k3s-server.bootstrap = "clan-vars" | "cloud-init"` to the module: `clan-vars` keeps today's behavior; `cloud-init` enables `services.cloud-init` with the NoCloud datasource, sets `services.k3s.configPath = "/etc/rancher/k3s/config.yaml"`, drops `--disable-cloud-controller`, and installs the `/opt/install.sh` shim from the store — verify: `nix eval` of both variants shows the differing `ExecStart`; `k3s-server-eval` is extended to assert both and passes
- [ ] S3.2 Add a Nix function rendering cluster-api-k3s' `airGapped` control-plane-init `user-data` (`write_files` for `config.yaml` and `KThreesConfig.spec.files`, `runcmd` `INSTALL_K3S_SKIP_DOWNLOAD=true INSTALL_K3S_EXEC='server' sh /opt/install.sh`), and a T1 assertion comparing the `runcmd` text against the pinned source's test fixture — verify: the T1 leaf passes; bumping the fixture text makes it fail
- [ ] S3.3 Add `modules/checks/vm-k3s-capi-bootstrap.nix` (A3): one `bootstrap = "cloud-init"` machine with a sandbox-built `cidata` ISO from S3.2 — verify: `cloud-final.service` completes, `k3s.service` is active with `--config /etc/rancher/k3s/config.yaml`, the sentinel file exists, `kubectl get nodes` lists the machine; removing the `config.yaml` `write_files` entry makes the shim exit non-zero and the leaf fail; revert
- [ ] S3.4 Render CAPI core, CAPH, and cluster-api-k3s provider manifests from Nix and write the `clusterctl.yaml` override pointing at their store paths — verify: `clusterctl init --config <override> --dry-run`-equivalent (`clusterctl generate provider`) succeeds offline for all three; the T1 purity leaves pass over the provider manifests
- [ ] S3.5 Add handler A: a `just capi-mgmt-a` recipe that runs the k3s node closure as a QEMU VM through `nixpkgs.virtualisation.host.pkgs`, installs the providers from S3.4, and writes a kubeconfig — verify: from `aarch64-darwin` (stibnite) and from `x86_64-linux` with KVM, `kubectl get providers -A` lists the three providers; timing recorded against R9.f
- [ ] S3.6 Add handler B: a `just capi-mgmt-b` recipe reusing the ctlptl recipes with `kubernetes/clusters/local-k3d/` stripped to CAPI controllers — verify: from `x86_64-linux` without KVM, `kubectl get providers -A` lists the same three providers and versions as S3.5
- [ ] S3.7 Add `modules/checks/vm-capi-management.nix`: handler A as a VM leaf with the providers from S3.4 and `kubectl apply --dry-run=server` of the rendered Hetzner `Cluster` CRs — verify: the leaf passes; rendering a `Cluster` with an unknown `infrastructureRef.kind` makes the dry run fail; revert
- [ ] S3.8 Add the Hetzner node-image derivation (disko + `k3s-server` with `bootstrap = "cloud-init"`) and `apps.<system>.upload-node-snapshot` (E) that uploads it and labels it `caph-image-name=k3s-<flake-rev-short>`, asserting the label read back; do not run it against Hetzner in this stage — verify: `nix build` of the image passes; the effect's label-assertion path is exercised against a mock API response

## S4. Two-node Hetzner deployment (requires explicit spend approval, A9)

- [ ] S4.1 Obtain Cameron's explicit written approval to create Hetzner resources — verify: the approval is quoted in the S4 PR body; without it no S4 task runs
- [ ] S4.2 Add the Clan `wireguard` instance for the Kubernetes admin plane (A5): control-plane nodes as controllers, stibnite as a peer, ZeroTier untouched — verify: `clan vars generate` produces the ULA and keys; the rendered `KThreesControlPlane` has the ULA in `tlsSan`; `rg zerotier modules/nixos/k3s-server/` returns nothing
- [ ] S4.3 Run `upload-node-snapshot` and `push-cluster-artifact` — verify: both exit zero with equal digests and matching label in the log
- [ ] S4.4 From handler A or B, apply the rendered `Cluster` CRs for `platform = hetzner` with one control-plane and one worker — verify: `kubectl get nodes` against the workload kubeconfig lists two `Ready` nodes within the runbook timeout; Flux root `Kustomization` `Ready=True` at the S4.3 digest; Cilium WireGuard `cilium encrypt status` reports WireGuard between the two node IPs
- [ ] S4.5 Bump the flake revision, re-run S4.3, update `HCloudMachineTemplate.spec.imageName` and the `KThreesConfig.spec.files` digest, and observe one node roll — verify: the worker is replaced with a node running the new closure and Flux converges at the new digest; the control-plane roll behavior is recorded against A1
- [ ] S4.6 Record the S4 runbook, costs, and every R9.c–R9.h observation in ADR-009's verified-vs-inferred table — verify: the table has no row still marked inferred without a linked observation or a remaining regulator

## S5. GCP/AWS render-only golden tests

- [ ] S5.1 Fill the `gcp` and `aws` platform submodules with `*Cluster`, `*MachineTemplate`, node-image reference, and CCM objects — verify: `capi-cloud-invariant-render`, `capi-platform-sum-total`, `capi-ccm-present` pass for all three variants
- [ ] S5.2 Add golden renders under `kubernetes/tests/golden/<platform>/` and a T1 leaf diffing the rendered tree against them — verify: the leaf passes; changing a `MachineDeployment` replica count fails it with a diff; revert
- [ ] S5.3 Declare a second meshed cluster for `gcp` with a disjoint PodCIDR under the shared native-routing CIDR — verify: `clustermesh-preconditions` passes; overlapping the PodCIDR fails it; revert

## Execution wiring and deletion (after S2, before S4 deletion of k3d)

- [ ] X.1 Add a `workflow_dispatch`-only probe job to `.github/workflows/test-cluster.yaml` on `ubuntu-latest` that builds `vm-k3s-single-node` with `--option system-features 'kvm nixos-test benchmark big-parallel'` — verify: three consecutive dispatches pass with run links recorded, or the failure is recorded and VM leaves remain developer-host regulators
- [ ] X.2 If X.1 passed, add a `vm` job building every `checks.x86_64-linux.vm-*` leaf through `cached-ci-job` with `hash-sources` covering `modules/checks/vm-*.nix modules/nixos/k3s-server/** kubernetes/** ` — verify: the job passes; a sysctl change in `kernel.nix` produces a cache miss
- [ ] X.3 In one commit separate from X.2, only after `vm-k3s-platform` has passed on the chosen runner and the first Flux SOPS cutover has converged: delete the k3d CI scripts, `scripts/k3d-test-coverage.sh`, the `integration` job and `SOPS_AGE_KEY` line, `modules/nixidy.nix`, `kubernetes/nixidy/`, the ArgoCD and sops-secrets-operator manifests, and the CI-only k3d justfile recipes; keep `kubernetes/clusters/local-k3d/` and the ctlptl recipes for handler B — verify: `rg k3d-integration-ci .github justfile modules scripts` and `rg nixidy modules kubernetes justfile` return nothing; reverting the commit restores the k3d job with the `vm` job unaffected
- [ ] X.4 Update `docs/notes/development/kubernetes/` (README, `workflows/06-testing.md`, `components/nixos-k3s-server.md`) and mark ADR-006 superseded by ADR-008 — verify: `prek run --all-files` passes and the docs name every `vm-*` leaf that exists

## Integration Verification

- [ ] IV.1 On the KVM developer host, `direnv exec . just test-integration 2>&1 | tee logs/test-integration-$(date +%Y%m%d-%H%M%S).log` builds every `vm-*` leaf from a clean store and each passes — verify: the log ends with each leaf's store path
- [ ] IV.2 `nix eval .#checks.aarch64-darwin --apply builtins.attrNames` lists no `vm-*` leaf, and `nix eval .#checks.x86_64-linux --apply builtins.attrNames` lists every T1 leaf named in S0 — verify: command output
- [ ] IV.3 The buildbot worker's evaluation of the final branch reports every non-VM check with an unchanged verdict, every S0 T1 leaf passing, and no VM leaf as a failure — verify: `ci-log-verification` on the buildbot run
- [ ] IV.4 No `checks.<system>.*` derivation references an `apps` publishing script — verify: `nix path-info -r` over the checks closure contains none of the `push-cluster-artifact` or `upload-node-snapshot` store paths
- [ ] IV.5 The mutation evidence for S0–S3 is present in the merged PR bodies and each mutated file is byte-identical to `main` after revert — verify: PR body links and `git diff main -- modules/nixos/k3s-server/ kubernetes/tests/` empty
- [ ] IV.6 Every requirement in the six delta specs is discharged by a named leaf, effect, recipe, or review rule with a coverage bin and non-vacuity evidence, and every world assumption is cited by the requirements that rest on it — verify: `direnv exec . openspec validate k3s-nixos-vm-tests` passes and a traceability pass over the specs finds no undischarged requirement
- [ ] IV.7 ADR-009's verified-vs-inferred table has every row either observed (with the S4 log) or still guarded by a named regulator — verify: no row reads "inferred" without a regulator column entry
