## 1. Phase 1 — Flake input scaffolding

- [x] 1.1 Add `nixos-hardware` to `flake.nix` inputs with `inputs.nixpkgs.follows = "nixpkgs"`, mirroring the disko declaration at `flake.nix:83-84`
- [x] 1.2 Confirm `nix flake check` still passes with the input added and unused, before any machine imports it

## 2. Phase 2 — The pyrite host module

- [x] 2.1 Create `modules/machines/nixos/pyrite/default.nix` exporting `flake.modules.nixos."machines/nixos/pyrite"`, shaped after `modules/machines/nixos/electrum/default.nix` (the fleet's only UEFI/systemd-boot host), importing `base`, `hm-sops-bridge`, `ssh-known-hosts`, home-manager, and `nixos-hardware.nixosModules.apple-macbook-pro-14-1`
- [x] 2.2 Set `networking.enableB43Firmware = false` and `hardware.facetimehd.enable = false` — plain `false`, not `mkForce`, since both upstream values are `lib.mkDefault` — with a comment recording that b43 is a misdetection for BCM4350/brcmfmac silicon rather than an evaluation workaround
- [x] 2.3 Set `boot.loader.systemd-boot.enable = true`, `boot.loader.efi.canTouchEfiVariables = true`, `boot.zfs.devNodes = "/dev/disk/by-id"`, `networking.hostName = "pyrite"`, and `system.stateVersion`
- [x] 2.4 Wire the admin user `cameron` exactly as electrum does: `hm-sops-bridge.users.cameron = { };` and `home-manager.users.cameron = { imports = flakeUsers.cameron.modules; };` — cameron is the preferred username on new machines and folds to crs58 by alias
- [x] 2.5 Disable `boot.initrd.network.enable`, with `lib.mkForce` because `base` sets it plainly. Initrd networking cannot function on a `brcmfmac`-only machine and this machine has no ethernet port at all. Disabling it subsumes the ssh sub-option — `initrd-ssh.nix:16` defines `enabled` as `(network.enable || systemd.network.enable) && cfg.enable`, so both disjuncts go false — and it is the option that matters for a second reason this task originally missed: it is the predicate `hardware/facter/networking/initrd.nix:18` gates on when it force-loads `brcmfmac` into the initrd, which unbinds the PCI device permanently and shipped the first boot with no wifi device. This task originally read "disable `boot.initrd.network.ssh`", the implementation did exactly that, and it left initrd networking on; see 7.7 and the design's `base`-initrd-assumptions section. Do NOT touch `boot.initrd.kernelModules`: `lib.mkForce` on that list outranks all five sources at once, so it would discard the profile's SPI modules, both `i915` definitions, or all of them, leaving a passphrase prompt that is unanswerable, invisible, or both. `base`'s virtio entries are inert on bare metal and are left alone
- [x] 2.6 Set `hardware.enableRedistributableFirmware = true` and `hardware.cpu.intel.updateMicrocode = true` explicitly, rather than inheriting the `mkDefault` values facter's bare-metal branch supplies, which are dead on every existing machine. Both are `true` deliberately, and neither is a candidate for the closure-minimization 2.2 applies to b43 and facetimehd: `enableRedistributableFirmware` is what puts `linux-firmware` into `hardware.firmware` (`nixos/modules/hardware/all-firmware.nix:71-86`, linux-firmware at `:75`), and `linux-firmware` carries the `brcm/brcmfmac4350*-pcie.bin` blobs this machine's only NIC needs in order to probe — setting it false darkens the WiFi, and its own default is `config.hardware.enableAllFirmware`, i.e. false, so nothing else stops that. The comment must record that the axis is redistributability and not freeness — `linux-firmware` is a proprietary blob that caches serve — so a later reader does not read 2.2 as a rule against firmware. See D15
- [x] 2.7 Keep `base`'s `boot.zfs.forceImportRoot = true` and comment why it is load-bearing for a re-runnable install: an install re-run touches the pool from the installer, and importing without force after that lands the next boot in an emergency shell
- [x] 2.8 Set no `networking.hostId`; clan-core's `mkDefault "8425e349"` matches the installer and nixos-anywhere by design
- [x] 2.9 Do not enable `boot.plymouth`; it swaps the ask-password agent away from the verified console path
- [x] 2.10 Set `services.mbpfan.aggressive = false` — the only mbpfan line the module needs. `nixos-hardware`'s `apple/default.nix:6` already sets `services.mbpfan.enable = lib.mkDefault true`, and the module's own `aggressive` default is `true` (`nixos/modules/services/misc/mbpfan.nix:28`), which takes the three temperature thresholds to 55/58/78 rather than 63/66/86 and spins the fans earlier; `false` is the quieter setting and is the only user-visible consequence of the daemon. Do not set `services.mbpfan.enable` or `services.tlp.enable`. `services.mbpfan.enable` already evaluates true from the profile's `mkDefault` (`apple/default.nix:6`), so a plain `true` would echo a default rather than change anything — unlike 2.6's firmware values, which D15 states because a live path leads to the wrong one. `services.tlp.enable` evaluates false: `common/pc/laptop/default.nix:6` sets it `lib.mkDefault (!config.services.power-profiles-daemon.enable)` and the GNOME desktop (2.11/D19) enables power-profiles-daemon, which supersedes tlp — the module leaves tlp to that resolution and forces nothing. The comment must record that mbpfan is not a candidate for the declines 2.2 applies: `mbpfan.meta.license` is `gpl3`, so it is free, redistributable, in-tree, and cache-served, while both of 2.2's declines are non-redistributable, which is the axis D5 and D15 both turn on. It also drags nothing in — the profile's initrd list already force-loads `applesmc` (`apple/macbook-pro/14-1/default.nix:23`), and the only module mbpfan adds beyond what this change already wants is `coretemp` (`mbpfan.nix:92-95`). See D16
- [x] 2.11 Enable the GNOME desktop in the host module: set `services.displayManager.gdm.enable = true` and `services.desktopManager.gnome.enable = true`, the two lines nixpkgs seeds into `nixos-generate-config` (`nixos/modules/services/desktop-managers/gnome.nix:250-251`). These are system-level and self-contained — they cascade the display manager, the XDG portals, the graphical polkit agent, gnome-keyring, dconf, `gnome-settings-daemon`, gnome-control-center, the NetworkManager applet, and gnome-shell — and a stock GNOME session needs nothing from cameron's home-manager, which 2.4 already imports, so no home-manager desktop configuration is added. Do not enable `boot.plymouth`: 2.9 and D11 stand, and GDM is a stage-2 display manager that does not touch the initrd passphrase path. niri is out of scope and is deferred to a reversible follow-up. See D19

## 3. Phase 3 — The disko layout

- [x] 3.1 Create `modules/machines/nixos/pyrite/disko.nix` assigning into `flake.modules.nixos."machines/nixos/pyrite"` with no import statement, following the sibling-file auto-merge pattern every existing machine uses
- [x] 3.2 Set `device = "/dev/disk/by-id/nvme-APPLE_SSD_AP0512J_C08843605KKHV4MAK_1"` with a comment recording the 8 KiB `nvme0n2` namespace reachable at the `_2` suffix — this is a footgun a reader cannot recover from the path itself
- [x] 3.3 Set `zpool.zroot.options.ashift = "12"` with a comment recording the disk's 4096-byte logical sector size, which no other machine in the fleet has and which disko does not detect
- [x] 3.4 Declare the ESP with `type = "EF00"` and `size = "1G"`, vfat, mounted at `/boot`, with a comment recording that Apple's EFI discovers the ESP by partition type and that disko's default is `8300` — the failure surfaces only after macOS is wiped
- [x] 3.5 Declare the ESP's sibling partition `zfs` with `size = "100%"` and `content = { type = "zfs"; pool = "zroot"; }`, matching the fleet's form at `modules/machines/nixos/electrum/disko.nix:22-28`. This is the partition that registers the vdev into `$disko_devices_dir/zfs_zroot`; without it the `zpool.zroot` block below has no device and pool creation fails after the wipe has already destroyed macOS
- [x] 3.6 Declare the `zroot` datasets `root`, `root/nixos`, `root/home`, `root/nix`, matching the fleet's layout
- [x] 3.7 Add `clan.core.vars.generators.zfs` with `files.key.neededFor = "partitioning"`, `runtimeInputs = [ pkgs.coreutils pkgs.xkcdpass ]`, and a script emitting a human-typeable passphrase after clan-infra `machines/build01/disko.nix:71-80` (`xkcdpass --numwords 6 --random-delimiters --case random`). Not hex — a person types this at a boot prompt. The script MUST write to `$out/key`, matching the `files.key` attribute the layout reads as `files.key.path`; build01 writes `$out/password` because its file attribute is `files.password`, and copying that literal yields a `clan vars generate` failure. Do NOT append build01's `| tr -d "\n"`; that trim exists for build01's consumer, `cryptsetup --key-file`, which does not trim, whereas ZFS trims one trailing newline for non-RAW keyformats on both the file and prompt paths
- [x] 3.8 Set the `root` dataset's `encryption = "aes-256-gcm"`, `keyformat = "passphrase"`, and create-time `keylocation = "file://${config.clan.core.vars.generators.zfs.files.key.path}"`
- [x] 3.9 Add `postCreateHook = ''zfs set keylocation="prompt" "zroot/root"'';` to the `root` dataset, with a comment recording that the create-time `/run/partitioning-secrets` path does not exist at boot, so the flip is what makes the machine bootable
- [x] 3.10 Confirm `nix eval .#nixosConfigurations.pyrite.config.system.build.toplevel.drvPath` succeeds — the machine must evaluate before any hardware detection or install runs

## 4. Phase 4 — Registration

- [x] 4.1 Add `pyrite` to `modules/clan/machines.nix`
- [x] 4.2 Add the `pyrite` entry to `modules/clan/inventory/machines.nix` with `machineClass = "nixos"`, `deploy.targetHost = "root@pyrite.zt"`, and tags `nixos`, `laptop`, `peer`
- [x] 4.4 Add `roles.default.machines."pyrite" = { };` to `modules/clan/inventory/services/users/cameron.nix`
- [x] 4.5 Create `modules/clan/inventory/services/wifi.nix` instancing clan-core's wifi service for pyrite. import-tree discovers the file, so no import line is added; follow `modules/clan/inventory/services/zerotier.nix`'s shape, including a header comment describing the service. The content is:

  ```nix
  {
    clan.inventory.instances.wifi = {
      module = {
        name = "wifi";
        input = "clan-core";
      };
      roles.default.machines."pyrite" = {
        settings.networks.fleet = { };
      };
    };
  }
  ```

  The attribute name under `networks` is an internal identifier and not the SSID — the SSID is a prompted var (`clanServices/wifi/default.nix:76-90`), because the NetworkManager profile is a world-readable store path and upstream cannot inline it. `fleet` is the internal identifier for whichever network the fleet uses, and the operator decided that network is the pre-existing household network `furtadosmith` (D14), whose SSID and PSK 5.2's prompts take. The name is a clan-wide identity rather than a per-machine one — the generator it produces carries `share = true` (`:88`), so `wifi.fleet` names exactly one credential across the clan — which is the intent here and is why a future machine on a different network takes a differently-named network rather than a different value under this name. Targeting is machine-scoped rather than tag-scoped because no `wireless` tag exists and pyrite is the sole taker. This supersedes the `wlp2s0` networkd unit an earlier revision carried as 2.10: NetworkManager manages the interface and forces `networking.useDHCP = false` (`nixos/modules/services/networking/networkmanager.nix:690`), so a hand-written `.network` for the same link would compete with it. No edit to `users/cameron.nix`'s `groups` list is needed — `networkmanager` is already there (`:25-28`) and 4.4 already adds pyrite to that instance. See D14
- [x] 4.6 Add `"pyrite"` alphabetically to both hardcoded lists in `modules/checks/structure/flake-shape.nix` (after `magnetite`, before `rosegold` in the inventory list; after `magnetite`, before `scheelite` in the nixosConfigurations list)
- [x] 4.7 Move `openspec/changes/pyrite-baremetal-nixos/pyrite-facter.json` to `machines/pyrite/facter.json` and git-track it — in the same commit as 2.1, 3.1, 4.1, 4.2, and 4.6, never before them, because creating `machines/pyrite/` alone injects an unconfigured inventory machine via clan-core's readDir scan
- [x] 4.8 Confirm `nix flake check` passes, including the auto-emitted `nixos-pyrite` toplevel build

## 5. Phase 5 — Secrets and ZeroTier vars

- [x] 5.1 No router work is required. The fleet network is the pre-existing household network `furtadosmith`, which the operator decided to use as the fleet network rather than standing up a distinct SSID; the `fleet` attribute in 4.5's instance is the internal identifier for it. It is already broadcasting with internet egress and its security matches the service's `keyMgmt` default of `wpa-psk`, which `clanServices/wifi/default.nix:35-43` describes as "WPA2 + WPA3 personal". Its SSID and PSK were entered at 5.2's two prompts, so no PSK is generated for this purpose and nothing here is a prerequisite 5.2 waits on. The one hazard that survives the decision is 5.2's, and it is unchanged: the operator types the SSID and nothing downstream catches a typo, which surfaces at 7.7 as an unassociated interface with no diagnostic distinguishing it from a radio failure. Correcting a mistyped SSID or PSK requires `clan vars generate pyrite --regenerate`, because a plain re-run does not re-prompt — `clan_cli/vars/generate.py:32` sets `auto_accept_prompts = not args.regenerate if args.regenerate is not None else True` and `clan_lib/vars/generate.py:247` then reuses the previous value. See D14
- [x] 5.2 Run `clan vars generate pyrite` and commit the generated vars, sops machine key, ZeroTier identity/IP, and the zfs passphrase. The wifi service adds two interactive prompts to this command: `network-name` (`type = "line"`, echoed) and `password` (`type = "hidden"`), declared at `clanServices/wifi/default.nix:76-90`. Both take the fleet SSID and PSK that 5.1 put on the router; neither is generated here, because the service declares prompts with `persist = true` and no script, which `modules/clan/export-modules/generic-generator.nix:239-245` documents as equivalent to `files.<name>.secret = true` plus `script = "cp $prompts/<name> $out/<name>"`. Both prompts belong to one per-network generator, `wifi.fleet`, carrying `share = true` (`:88`), so they land under `vars/shared/wifi.fleet/` rather than `vars/per-machine/pyrite/` and name one credential clan-wide. Get them right on the first pass: a re-run does not re-prompt, because `clan_cli/vars/generate.py:32` sets `auto_accept_prompts = not args.regenerate if args.regenerate is not None else True` and `clan_lib/vars/generate.py:247` then reuses the previous value, so correcting a mistyped SSID or PSK requires `--regenerate`. 5.2's place in the sequence is ordering rather than a hard prerequisite of the install: clan runs these generators itself at install time and would prompt for anything missing, so what ordering 5.2 ahead of the install secures is that the sops recipients (tasks 5.4–5.5) are in place before any secret is minted, not the mere existence of the vars
- [ ] 5.3 Record the generated passphrase in the password manager under `pyrite/zfs-root` before the first boot, and verify the entry matches `clan vars get pyrite zfs/key`. The install writes it but a person types it at every boot (D1), and the var 5.2 committed at `vars/per-machine/pyrite/zfs/key/secret` is encrypted to exactly one human key — `age1vn8fpkmkzkjttcuc3prq3jrp7t5fsrdqey74ydu5p88keqmcupvs8jtmv8`, aliased byte-identically under `sops/users/cameron/key.json` and `sops/users/crs58/key.json`, and a recipient of all 97 vars in the repository while `raquel`, `janettesmith`, and `christophersmith` are recipients of none and no `sops/groups/` exists — so a copy outside the repository is the only thing standing between a lost age key and a lost pool. pyrite's own machine key is also a recipient and contributes nothing to recovery: it lives at `/var/lib/sops-nix/key.txt` on the encrypted root. `clan vars get` appears here as the verification and not as the recording — it reads back the very var this step exists to duplicate, protected by the very key whose loss is the failure mode, so it is not a second medium and does not discharge the step. This is 5.1's shape applied to the disk passphrase, for the same reason: the vars are not a medium a person can read when the key that opens them is gone. See D1
- [x] 5.4 Add the `&pyrite` age anchor to `.sops.yaml` and `*pyrite` to the `secrets/bridge/.*` creation rule
- [x] 5.5 Run `just update-all-keys` to re-encrypt, and confirm the existing machines can still decrypt the bridge secret.
  The recipe was `fd -e yaml -e json . secrets/ -x sops updatekeys -y {}`, whose extension filter matched neither `secrets/bridge/crs58-age-key.enc` nor `tara-age-key.enc`, so it exited 0 while leaving 5.4's `*pyrite` rule inert over the bridge ciphertext.
  Resolved by widening the recipe to `fd -e yaml -e json -e enc . secrets/ -x sops updatekeys -y {}`, which fixes the defect class rather than the two instances and so covers future machine onboarding.
  The re-run rewrote both bridge files and left every `.yaml`/`.json` secret a no-op.
  Each bridge file now carries 9 age recipients including pyrite's `age1eajmgz9zvq639zjnmqcaklst6u3s7un8k68nd4klnnlswgtrnylq7twk4v`, both still decrypt with exit 0, and both plaintext digests are byte-identical to their pre-change values.
  The store copy nix resolves for `nixosConfigurations.pyrite.config.sops.secrets."crs58-age-key".sopsFile` is byte-identical to the worktree file, so the build consumes the pyrite-bearing bytes
- [x] 5.6 Run `clan machines update cinnabar` so the controller's autoaccept unit admits the new peer.
  Done, and it carried a real payload rather than going green on a same-revision no-op: `logs/cinnabar-20260718-181110.log` records `PATHS 3103 -> 3382` (+1.26 GiB) with dnsmasq restarted and the zerotier autoaccept unit started.
  cinnabar's `readlink /run/current-system` returns `/nix/store/b9xkivypkkdc4s6iz8sl8955gkixw4bj-nixos-system-cinnabar-26.11.20260703.dirty`, byte-identical to the evaluated toplevel.
  The admission is confirmed at the controller rather than inferred from the deploy: member `7e80678028` shows authorized at 2026-07-18 22:16:47 UTC

## 6. Phase 6 — The recorded install path

- [x] 6.1 Write the install path as a recorded, re-runnable artifact (justfile recipe and/or a documented runbook), whose first step discards the disk with `blkdiscard` against the `_1` by-id path — following clan-core's own form at `docs/src/guides/disk-encryption.md:84-88` — and which then invokes `clan machines install pyrite --target-host nixos@<installer-ip> -i <key> --yes` with `--update-hardware-config` left at its default of `none`. The target is `nixos@`, not `root@`: the stock installer autologins the `nixos` user (`installation-device.nix:61`), places it in `wheel` (`:40`), and gives it passwordless sudo (`:57`), while both `nixos` and `root` ship empty passwords (`:45`, `:49`) that sshd refuses for password auth — so `root@` would need a key hand-installed into `/root/.ssh` first, whereas `nixos@` needs only the key authorized in the GNOME session already open on the machine, and nixos-anywhere escalates to root through that passwordless sudo. The installed-system `deploy.targetHost = "root@pyrite.zt"` (task 4.2) is unrelated and stays: it deploys over ZeroTier to the installed machine's own host key. Use `blkdiscard`, not `sgdisk --zap-all`: zapping the GPT first leaves the disk with no children for `lsblk` to report, which is the only way disko's `disk-deactivate.jq:7-9` reaches `zpool labelclear`, so the ZFS labels inside p2 survive the wipe and the next install silently reuses the old pool. If `blkdiscard` proves unavailable, the fallback order is absolute — `zpool labelclear -f <device>-part2`, then `sgdisk --zap-all`, then `wipefs -a` — because the labelclear needs the partition table the zap destroys
- [x] 6.2 Record why the wipe is a step rather than an assumption. It is not, as an earlier revision claimed, because the run is create-only — the default disko mode destroys first (D8). It is belt-and-braces on the happy path, retained because the happy path is not guaranteed: `_legacyDestroy` runs without `set -e` so a failed destroy falls through to `_create` and lands in the boot-Apple's-ESP end state anyway; the wipe is the only step whose success the operator can observe before committing to an irreversible install; and clan-core's own encrypted-root guide prescribes a manual `blkdiscard` pre-wipe for exactly this scenario
- [x] 6.3 Document that `--disk-encryption-keys` is appended automatically by clan-cli from the `neededFor = "partitioning"` generator and is not passed by hand, and that `--yes` confirms the install but does not auto-accept the vars-generator prompts: the install path calls `run_generators` without `auto_accept_prompts` (`clan_lib/machines/install.py:116`), whose default is `False` (`clan_lib/vars/generate.py:271`), so an install run with Phase 5 incomplete stops interactively on stibnite asking for the SSID and PSK rather than failing — Phase 5.2 front-loads generation so the ordering is already right
- [x] 6.4 Document the ISO-boot prerequisites in the runbook: Option-key boot, sshd reachable, that the key is authorized for the `nixos` account (`ssh-copy-id nixos@<installer-ip>`, writing `/home/nixos/.ssh/authorized_keys`) from the running GNOME session and does not survive rebooting the installer, and that the live ISO is itself the installer environment (`VARIANT_ID=installer`), so nixos-anywhere's kexec phase is a no-op — the running live-CD ssh access is exactly and sufficiently what the install consumes, which forecloses the "physical install, then update" misconception (nixos-anywhere pinned at 1.13.0 per D13)
- [x] 6.5 Document that `clan init`, `clan machines create`, and `clan templates apply disk` are all skipped, and why
- [x] 6.6 Document the build-host behaviour: stibnite's `magnetite-builder` is preferred but mesh-only, so an off-mesh install falls back to the local Rosetta builder
- [x] 6.7 Document the USB-keyboard recovery path, stating that a MacBookPro14,1 is USB-C only and that a USB-C keyboard or adapter must be on hand before the first boot rather than sourced after a failure

## 7. Phase 7 — Install and post-install

- [ ] 7.1 Boot the stock installer ISO on the `NixOS 26.05.5092.4382ed2b7a68 Installer GNOME (Linux LTS)` entry and authorize a key for the `nixos` account — `ssh-copy-id nixos@<installer-ip>` from the running GNOME session, which writes `/home/nixos/.ssh/authorized_keys`; the authorization does not survive an installer reboot. The entry is not interchangeable with the others the menu offers: the two `*_latest_kernel` specialisations import `nixos/modules/installer/cd-dvd/latest-kernel.nix`, whose `:4` sets `boot.supportedFilesystems.zfs = false`, so booting one of them and proceeding runs 7.3's `blkdiscard`, destroys macOS, and then fails at `zpool create`. See D18 and the runbook's boot section.
  Performed once already, by appending https://github.com/cameronraysmith.keys to `/home/nixos/.ssh/authorized_keys` with `curl` from the machine's own session rather than by `ssh-copy-id` from the admin box; both forms write the same file and either discharges the step.
  It stays unchecked because that authorization was made on a `7.1.3` entry and does not survive the reboot onto the LTS entry this task now requires — the key lives in the live filesystem and is not written to the installer media — so it must be redone after that reboot, together with the root authorization the runbook adds
- [ ] 7.1a Confirm the running kernel is the LTS one before anything destructive runs: `uname -r` reports `6.18.38`, not `7.1.3`. This is the check that distinguishes the two entries after boot, since the GRUB menu labels are gone by then, and it is cheaper than the two positive checks it stands in for — `modprobe zfs` succeeding and `command -v zpool` resolving, both of which fail on a `7.1.3` entry. Reboot onto the LTS entry and repeat 7.1's key authorization if this reports `7.1.3`
- [x] 7.2 Confirm `blkdiscard` and `blockdev` are on the installer's PATH, and record `blockdev --getss /dev/nvme0n1` — the logical sector size the `ashift = "12"` decision assumes. This is a two-command check on a machine that is about to be destroyed irreversibly, and it forecloses the `dd bs=440` question in D8 rather than reasoning about it. It is also the last non-destructive step: the point of no return is the `blkdiscard` that opens 7.3, not anything in 7.4.
  Done: `blkdiscard`, `blockdev`, and `sgdisk` all resolve on the installer, and `blockdev --getss /dev/nvme0n1` returns 4096 with `--getpbsz` also 4096, confirming the 4096-byte logical sector size the `ashift = "12"` decision assumes.
  Both measurements were taken on a `7.1.3` entry. Neither depends on the kernel — `util-linux` reaches every NixOS system-path unconditionally and the sector size is a property of the disk — but re-confirm both on the LTS entry alongside 7.1a, since that boot happens anyway and the check is two commands
- [ ] 7.3 This is the point of no return. It opens with the `blkdiscard` wipe and then runs the full recorded install path as written — `clan machines install pyrite --target-host nixos@<installer-ip> -i <key> --yes`, with nixos-anywhere's default phases and no `--phases` flag of any kind. macOS is destroyed here, by the wipe and then by disko's `_legacyDestroy → _create → _mount` (D8). The naive `--phases disko` form an earlier revision of this task carried is forbidden and MUST NOT be reintroduced: `--phases` zeroes all four phases and sets only the named ones, so `phases[kexec]=0`, and `src/nixos-anywhere.sh:978-983` then unconditionally rewrites `sshConnection="root@${sshHost}"` whenever `phases[kexec] != 1` — before `uploadSshKey` at `:983`. `uploadSshKey` (`:527-566`) wraps `ssh-copy-id` in an `until ... sleep 3` loop with no abort, `installation-device.nix:48` gives root an empty `initialHashedPassword` that sshd refuses, and root carries no authorized_keys, so the run hangs forever on "Uploading install SSH keys" with no error — after the irreversible wipe. Keeping the default phases costs nothing: `kexec` returns immediately against an installer ISO (`:694-697`, `isInstaller == "y"`), so the phase is a no-op that preserves the `nixos@` connection the whole path depends on. The runbook has forbidden this at its "Boot the installer" section and D13 states the same mechanism; this task was the defective artifact
- [ ] 7.4 Verify the disk story the install committed to, against the machine 7.3 left behind. `sgdisk -p /dev/nvme0n1` shows the declared geometry — a 1G `EF00` ESP plus a 100% sibling. Then verify the pool was created by this run rather than inherited from a previous one: `zpool history zroot` opens with a `zpool create` entry whose timestamp falls inside this install session, and record `zpool get -H -o value guid zroot` for 7.12 to compare against. This runs after the full install rather than after a disko-only stage, because there is no disko-only stage: the separate `--phases disko` run that used to carry these checks is what 7.3 forbids, and splitting the install to keep an earlier checkpoint would reintroduce it.

  The check is `zpool history` and not the bare `zpool import` an earlier revision of this task specified, because that one could not fail. disko's `_mount` runs `zpool list "zroot" >/dev/null 2>/dev/null || zpool import -l -R /mnt "zroot"` (`lib/types/zpool.nix:409-410`), and `zpool export` appears only in `_unmount` (`:424`), which `_disko`'s `_legacyDestroy → _create → _mount` composition never reaches — so `zroot` is imported when the check runs. A bare `zpool import` with no arguments lists only pools available *for* import, which an already-imported pool is not, so it reports nothing on the pass path and nothing on the fail path alike. It was a tautological green of exactly the kind D8 refuses.

  `zpool history` is severe against the specific failure this change is built to catch. ZFS stores the history inside the pool, so it is a provenance record that survives export and import rather than a log of the current session. Under the surviving-pool failure mode — the one 7.12 exists to detect, where `blkdiscard` did not take and `zpool.nix:298`'s `zpool import -N -f "zroot"` succeeds so `:299` logs "not creating zpool zroot as a pool with that name already exists" — no `zpool create` runs at all, the history's opening entry is the *previous* install's create carrying that older timestamp, and this run appears only as import and `zfs set` entries. The check fails there and passes only where a create actually happened, which is the discrimination the old check silently declined to make. The recorded GUID is the cross-run corroboration: `zpool create` mints a fresh pool GUID, so a 7.12 run reporting the same GUID as this one reused the pool whatever else it reports
- [x] 7.5 Confirm the machine boots, the stage-1 passphrase prompt appears on the internal keyboard, and the root unlocks with the generated passphrase.

  Observed on the installed system: the machine boots, the stage-1 prompt renders on the internal Retina panel, it accepts input from the internal SPI keyboard, and the generated passphrase unlocks the root. The SPI input path and the framebuffer both survive the initrd change, which is the property the design's `mkForce` invariant protects
- [x] 7.6 Confirm `zfs get keylocation zroot/root` returns `prompt`, `zfs get keyformat zroot/root` returns `passphrase`, and `zpool get ashift zroot` returns `12`.

  All three read back from the running machine with the expected values, so the create-from-keyfile-then-flip idiom of D1 landed and D2's `ashift` reached the pool
- [ ] 7.7 Confirm `wlp2s0` associated unattended with the fleet SSID, with no credential typed into the installed system, and that the machine joins the ZeroTier mesh. This is an observation and it can fail: the vars carry the credentials, so nothing in this step supplies them. It holds only if 5.1 put the SSID on the router and 5.2 ran and its output was committed before the deploy — if the vars are absent the NetworkManager profile interpolates empty strings and association fails silently, with no assertion and no eval-time error that would have caught it earlier, and if the SSID is wrong or the network is not broadcasting the interface simply never associates. `nmcli connection show --active` and `nmcli device wifi list` separate a credential failure from an absent network, and `journalctl -u NetworkManager-setup-secrets-wifi` distinguishes both from a radio or firmware failure.

  Run `ip -br link`, `nmcli device status`, and `lsmod | grep brcm` first, ahead of all three of those. This task failed on the first boot, and it failed in a way its own stated diagnostics could not name: there was no `wlp2s0` in `nmcli device status` or `ip -br link` at all, and every diagnostic this task listed presupposes the device exists. `nmcli connection show --active` and `nmcli device wifi list` report on a device, and `journalctl -u NetworkManager-setup-secrets-wifi` reports on the unit that plants the profile, which ran correctly; none of them distinguishes an unassociated interface from an absent one. The device-existence check is the prior discriminator and it belongs first. This is not a defect in the task: it was written to catch a silent association failure and it caught a driver failure instead, which is the outcome a check written this way is supposed to produce.

  The cause was a driver failure rather than a firmware or credential one. `lsmod` showed `brcmfmac` loaded with no `brcmfmac_bca` — the 6.18 per-vendor sub-module this chip needs, absent from the shrunken initrd module tree, so the stage-1 `request_module` failed and `brcmf_fwvid_attach`'s error path unbound the PCI device permanently. Fixed by `boot.initrd.network.enable = lib.mkForce false` in the host module, which falsifies the gate `hardware/facter/networking/initrd.nix:18` uses to put `brcmfmac` in the initrd. See the design's `base`-initrd-assumptions section.

  The substance then passed, against a manual `modprobe brcmfmac_bca` plus a rebind of `0000:02:00.0`: `wlp2s0` appeared and NetworkManager associated with the fleet SSID with no credential typed into the installed system. The active profile is `/var/run/NetworkManager/system-connections/fleet.nmconnection`, which is clan's `ensureProfiles` path, and `/etc/NetworkManager/system-connections` is empty — so the credentials came from the committed vars and from nowhere else, which is what this task exists to establish. It stays unchecked because that evidence rests on a manual bind that does not survive a reboot; 7.16 discharges it unattended
- [x] 7.8 Confirm the tor daemon is running and the onion service is published — `systemctl is-active tor` returns `active`, and `/run/tor/onion/clan_tor/hostname` exists and matches the `hostname` var — and that sshd host certificates are present. Both arrive from the `nixos` tag, which selects the tor server role alongside sshd's; D10 takes the role deliberately rather than by accident. The tor half can fail independently and it depends on 5.2: the daemon reads its onion key from `/run/secrets/vars/tor_tor/hs_ed25519_secret_key` (`clanServices/tor/default.nix:128`, whose `path` the machine evaluates to that sops-nix location), so if the `tor_tor` vars were not generated and committed before the deploy the unit fails to start with its secret absent. Note the onion directory is under `/run/tor` and not `/var/lib/tor`, because `nixos/modules/services/security/tor.nix:847` selects `runDir` over `stateDir` whenever `secretKey` is set, which the clan role always sets

  `systemctl is-active tor` returns `active`, `/run/tor/onion/clan_tor/hostname` exists and reads `k4m2zkpqgg37iwuwcmiroy7lyosvpxh7vmc4wuftaaq3kcqrx7qp6kad.onion`, and the sshd host certificate validates against the clan CA, checked from cinnabar rather than on pyrite itself so the validation does not rest on the machine presenting its own trust root
- [x] 7.9 Add the `/pyrite.zt/<address>` record to `modules/machines/nixos/cinnabar/zt-dns.nix` and redeploy cinnabar.
  The record is committed ahead of the install — `/pyrite.zt/fddb:4344:343b:14b9:399:937e:8067:8028`, read from the committed shared var `vars/shared/zerotier-ip-pyrite-zerotier/ip/value` — and cinnabar still evaluates.
  The redeploy half is discharged by the same cinnabar deploy 5.6 records, which restarted dnsmasq.
  Resolution is confirmed from the admin box rather than inferred: `pyrite.zt` resolves from stibnite to `fddb:4344:343b:14b9:399:937e:8067:8028`, the address the record declares
- [x] 7.10 Add the `pyrite.zt` entries to `modules/system/ssh-known-hosts.nix` (public key resolves automatically from the flake for NixOS machines; only the address literal is hand-written) and `modules/home/core/ssh.nix`. Pulled forward ahead of the install: both entries are repo edits with no on-hardware component, and the host public key resolves from the flake because 5.2 already generated and committed pyrite's openssh var
- [x] 7.11 Commit `inventory.json` if clan wrote `installedAt`.

  clan wrote it during the install and the update is committed
- [ ] 7.12 Re-run the install path a second time from a fresh ISO boot, including the `blkdiscard` and 7.4's checks, and repeat 7.6 — a re-run against a surviving pool reuses it and skips the create path entirely, so it would go green without proving anything
- [x] 7.13 After 7.5's unlock and once boot completes, confirm the machine reaches a graphical login: the GDM greeter renders on the internal Retina panel, selecting `cameron` and authenticating with the clan-generated user password (`clan vars get pyrite user-password-cameron/user-password`) reaches an interactive GNOME shell, and the Activities overview responds. This is a physical observation and it can fail: `systemctl is-active display-manager` returning `active` and `nix eval .#nixosConfigurations.pyrite.config.services.desktopManager.gnome.enable` returning `true` are both necessary but neither is sufficient — GDM reports `active` while `i915` KMS leaves the panel blank (the installed 6.18.37 kernel is not the ISO's 7.1.3 on which the panel was proven) and while a session restart-loops back to the greeter, so only a rendered greeter plus a reached, interactive shell discharges it. No audio is involved — sound is a Non-Goal — and no network is involved, which is the travel-readiness point: this login is observable with the mesh absent. See D19, Goal 5

  The GDM greeter rendered on the internal Retina panel and `cameron` reached an interactive GNOME session, which is the pair of observations this task requires; `i915` KMS drives the panel and no session restart-loop occurred
- [ ] 7.14 Confirm enabling GDM did not perturb the stage-1 passphrase prompt: 7.5's ZFS prompt still renders on the internal keyboard in initrd and unlocks before any graphical target starts. This holds structurally rather than by luck — the prompt is a stage-1 initrd event and `systemd.services.display-manager` is a stage-2 unit ordered after `systemd-user-sessions.service` (`gdm.nix:294-300`), reached only after the root the prompt gates is mounted, and GDM enables no plymouth (its plymouth coupling is guarded by `lib.mkIf config.boot.plymouth.enable`, `gdm.nix:313`), so D11's console ask-password path is untouched. The eval-side half is decidable without the hardware: `nix eval .#nixosConfigurations.pyrite.config.boot.plymouth.enable` returns `false`. A regression would show as a changed, absent, or differently-rendered stage-1 prompt; 7.5 passing on the same boot that reaches 7.13's greeter is the joint evidence. See D11, D19
- [ ] 7.15 Run the steady-state update in D17's recorded fleet form — `clan machines update pyrite 2>&1 | tee logs/pyrite-$(date +%Y%m%d-%H%M%S).log`, bare, with no `--target-host` and no `--build-host`. This is the travel-readiness acceptance check: every other Phase 7 step observes a machine on the bench, and this is the only one that exercises the steady-state update path used for all subsequent updates. It is appended rather than inserted because 7.12 destroys and recreates the pool, so the machine that must be reachable is the one 7.12 leaves behind — and both identity artifacts survive that re-install, the ZeroTier identity and address as committed shared vars (`vars/shared/zerotier-identity-<name>/`, `vars/shared/zerotier-ip-<name>-zerotier/`) and the ssh host key as a committed per-machine var (`vars/per-machine/<name>/openssh/ssh.id_ed25519/secret`, whose public half `modules/system/ssh-known-hosts.nix` reads back out of the flake rather than off the machine), so 7.9's record and 7.10's entry both stay valid across it. The whole chain must already hold: 4.2's `deploy.targetHost = "root@pyrite.zt"`, 5.1's SSID and 5.2's vars reaching 7.7's association, 5.2's ZeroTier IP var admitted by 5.6's cinnabar redeploy (D9 — without it the peer is never allowed onto the network), 7.9's dnsmasq record, 7.10's known-hosts, and root's authorized keys from `base`, which folds every wheel user's keys onto root at `modules/system/admins.nix:24-30`. Two things would take this green without proving reachability. Passing `--target-host root@<lan-ip>` bypasses 7.9 and the mesh together, so run it bare and confirm beforehand that `getent hosts pyrite.zt` returns the `fddb:` ZeroTier address — resolution to that address is what puts the traffic on the mesh, and it is the one link a bench LAN can silently substitute for. And a same-revision run carries no payload: `upload_sources` copies nothing and `nix build` on pyrite finds the toplevel already in the store, so the mesh moves no closure and the run proves only that the connection opened — deploy from a revision that actually changes pyrite's toplevel, and confirm `readlink /run/current-system` moved. That second guard is the blackphos precedent's own failure, and it is invisible here rather than merely unlikely: `logs/blackphos-20260711-195114.log:1,8-10` is a zero-diff run — `copying 0 paths`, `PATHS: 2270 -> 2270 (+0, -0)`, `DIFF: 0 bytes` — legible only because `darwin-rebuild` prints its own diff block, and pyrite takes `_update_nixos` instead (`clan_lib/machines/update.py:615`, `:637`), which never calls `nixos-rebuild` (`:567-569`) and prints no diff at all

- [ ] 7.16 Redeploy with the `boot.initrd.network.enable = lib.mkForce false` fix and confirm wifi comes up unattended across a reboot. This is the acceptance criterion 7.7 could not discharge: the working state 7.7 records rests on a manual `modprobe brcmfmac_bca` and a manual rebind of `0000:02:00.0`, neither of which survives a reboot, so the machine as it stands is one power cycle from being offline again with no other NIC to recover through. Deploy the fix, then reboot with nothing typed and nothing bound by hand, and confirm in this order: `lsmod | grep brcm` shows `brcmfmac` with `brcmfmac_bca` among its dependents; `ip -br link` and `nmcli device status` both show `wlp2s0`; and `nmcli connection show --active` shows the fleet profile active, resolving to `/var/run/NetworkManager/system-connections/fleet.nmconnection` with `/etc/NetworkManager/system-connections` still empty. The device checks come first for the reason 7.7 records — they discriminate an absent device from an unassociated one, and the association checks presuppose the device.

  Two ways this could go green without proving anything. A deploy that does not actually change pyrite's toplevel carries no payload, which is 7.15's trap and applies identically here, so confirm `readlink /run/current-system` moved. And the fix is only observable from a cold path: `brcmfmac` stays loaded and `wlp2s0` stays up across a `clan machines update` alone, so the manual bind's effects persist and the run would report success against the old initrd. The reboot is the test, not the deploy. Confirm the new initrd is the one that booted rather than a stale generation.

  The eval-side half is decidable before any of this and is already discharged: `nix eval --json .#nixosConfigurations.pyrite.config.boot.initrd.kernelModules` returns `["applesmc","applespi","dm_mod","i915","intel_lpss_pci","spi_pxa2xx_platform","virtio_net","virtio_pci","zfs"]`, with `brcmfmac` gone and the SPI input modules and `i915` intact. That is necessary and not sufficient: it establishes what the initrd contains and says nothing about whether stage 2 then loads the driver against the full module tree, which only the hardware answers. Re-run 7.5 on the same boot — the stage-1 prompt must still render on the internal keyboard and unlock, since the modules carrying it are the ones the initrd change must not have disturbed. See the design's `base`-initrd-assumptions section

## 8. Phase 8 — Do not update the flake before Phase 7

- [ ] 8.1 Do not run `nix flake update` between now and the install. `flake.nix:70` floats `clan-core` on `main.tar.gz`, so an update can move the transitive `nixpkgs_2` node and with it nixos-anywhere's version — currently 1.13.0, the version every mechanic in D13 was verified against — while no `nixos-anywhere` node appears in the diff, because `flake.lock` has none to move. If the flake must be updated first, re-verify D8 and D13 against the new version before Phase 7
